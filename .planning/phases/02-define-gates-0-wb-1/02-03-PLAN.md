---
phase: 02-define-gates-0-wb-1
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - skills/project/define/SKILL.md
autonomous: true
requirements: [DEF-01, DEF-02, DEF-03, DEF-04, DEF-05, DEF-06, DEF-07, DEF-08, DEF-09, DEF-10, DEF-11, DEF-12, DEF-13, DEF-14, DEF-15, DEF-16]
must_haves:
  truths:
    - "SKILL.md orchestrates Gates 0, WB, and 1 as a single continuous session (DEF-14)"
    - "SKILL.md detects greenfield and skips Gate 0 (DEF-01)"
    - "SKILL.md detects revision mode and jumps to Gate 1 revision flow (DEF-15)"
    - "SKILL.md loads each gate reference file only at the step that needs it (reference-loading pattern)"
    - "SKILL.md has disable-model-invocation: true in frontmatter"
    - "SKILL.md is under 350 lines"
  artifacts:
    - path: "skills/project/define/SKILL.md"
      provides: "Complete /define skill entry point with flow control across 3 gates"
      contains: "disable-model-invocation: true"
      min_lines: 150
  key_links:
    - from: "skills/project/define/SKILL.md"
      to: "references/gate-0-codebase.md"
      via: "Read tool at Gate 0 step"
      pattern: "references/gate-0-codebase"
    - from: "skills/project/define/SKILL.md"
      to: "references/gate-wb-working-backwards.md"
      via: "Read tool at Gate WB step"
      pattern: "references/gate-wb-working-backwards"
    - from: "skills/project/define/SKILL.md"
      to: "references/gate-1-prd.md"
      via: "Read tool at Gate 1 step"
      pattern: "references/gate-1-prd"
---

<objective>
Create the SKILL.md entry point for /define that orchestrates Gates 0, WB, and 1 as a single continuous session.

Purpose: SKILL.md is the flow controller. It handles mode detection (greenfield, revision, Gate WB resume), gate sequencing, and state transitions. Each gate's detailed logic lives in its reference file -- SKILL.md loads the relevant reference at each step using the Read tool.

Output: skills/project/define/SKILL.md (~300 lines)
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-define-gates-0-wb-1/02-CONTEXT.md
@.planning/phases/02-define-gates-0-wb-1/02-RESEARCH.md

@.planning/phases/02-define-gates-0-wb-1/02-01-SUMMARY.md
@.planning/phases/02-define-gates-0-wb-1/02-02-SUMMARY.md

<interfaces>
<!-- Reference files created by Plans 01 and 02 that SKILL.md will load -->

Files in skills/project/define/references/:
- gate-0-codebase.md -- Gate 0 full spec (scan, assessment, review, approval)
- gate-wb-working-backwards.md -- Gate WB full spec (offer, interview, PR/FAQ, review, approval)
- gate-1-prd.md -- Gate 1 full spec (context refresh, interview, PRD, partial approval, revision mode)
- review-checklist-template.md -- shared review checklist format for all gates
- progress-format.md -- local copy of progress.txt format spec

Files in skills/project/define/assets/:
- prd-template.md -- adapted PRD template (no Architecture/Features sections, has Milestones section)

<!-- Pattern reference from /project SKILL.md -->
From skills/project/SKILL.md:
- Frontmatter: name, description (with trigger phrases), disable-model-invocation: true
- Step pattern: numbered steps with clear action verbs
- Read tool loads references at each step (not all upfront)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /define SKILL.md with flow control and gate sequencing</name>
  <files>skills/project/define/SKILL.md</files>
  <read_first>
    skills/project/SKILL.md,
    skills/create-prd/SKILL.md,
    skills/project/define/references/gate-0-codebase.md,
    skills/project/define/references/gate-wb-working-backwards.md,
    skills/project/define/references/gate-1-prd.md,
    skills/project/DESIGN.md,
    .planning/phases/02-define-gates-0-wb-1/02-CONTEXT.md,
    .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
Create `skills/project/define/SKILL.md` targeting ~250-300 lines (under 350 max). The file follows the established SKILL.md pattern from `/project` and `create-prd`.

**YAML Frontmatter** (per CONVENTIONS.md, between triple-dash delimiters):
- `name: define`
- `description: >` followed by multi-line text: "Codebase assessment, optional Working Backwards, and PRD creation in a single session. Runs Gates 0, WB, and 1 continuously. Use when starting project definition, creating a PRD, assessing an existing codebase, or running the Working Backwards exercise. Phrases like 'define the project', 'create PRD', 'assess codebase', 'working backwards' are good triggers."
- `disable-model-invocation: true`

**Header:** `# /define -- Project Definition (Gates 0, WB, 1)` followed by a 2-line description: "Single-session skill that produces codebase assessment (Gate 0), optional Working Backwards document (Gate WB), and an approved PRD (Gate 1). All three gates run as one continuous conversation -- the user does not leave between gates (DEF-14, DD-2 exception)."

**Rules section** (following /project SKILL.md pattern):
- **Read fresh every time.** Read `progress.txt` from disk on every invocation (STATE-03).
- **Single continuous session.** Gates 0, WB, and 1 run in one conversation. User does not leave between gates (DEF-14).
- **Produce-then-review at every gate.** Produce the full artifact, present it, offer Approve/Revise. Rejection = revision request (D-06, D-08).
- **All checklist items must be resolved.** Every review checklist item must be `[x]` or `[-]` (N/A with reason) before recording gate approval (DEF-06, D-07).
- **Write-ordering contract.** When writing to state files, follow STATE-04 ordering. For `/define`, only `progress.txt` is modified (gate entries).
- **Interactive prompts.** Use `AskUserQuestion` for all user-facing choices (2-4 options, max 12-character headers).
- **No auto-dispatch.** Tell the user what to run next after completion. Never auto-invoke another skill.

**Prerequisites section:**
- Working directory is the project root (where `progress.txt` lives or will be created by `/project`)
- `progress.txt` must exist -- if it does not, instruct the user to run `/project` first
- Gate 0 must not already be approved (unless this is a revision mode invocation)

**Step 1 -- Detect Mode and State:**
Read `progress.txt` from the project root.

Revision mode detection (DEF-15):
- If `prd.md` exists AND Gate 1 is already `[x]` approved in progress.txt AND the user's message signals revision intent (keywords: "revise", "update PRD", "goals changed", "pivot", "change direction", "scope change")
- Jump to Step 6 (Revision Mode)

Greenfield detection (DEF-01, D-11):
Check ALL of the following (all must be true to classify as greenfield):
- No `src/`, `app/`, or `lib/` directories exist
- No dependency manifests exist (package.json, pyproject.toml, Cargo.toml, go.mod, requirements.txt, Gemfile, pom.xml, build.gradle)
- Fewer than 5 non-config files
- Only boilerplate files (README, license, gitignore pattern)

If ALL true: record Gate 0 as `[-] Gate 0: Codebase Alignment  Skipped (greenfield)` in progress.txt, skip to Step 4 (Gate WB Offer).
If ANY false: proceed to Step 2 (Gate 0).

Gate WB resume detection:
- If Gate WB is `[ ] Pending` in progress.txt (from a previous session where user deferred): proceed to Step 4 (Gate WB Offer) with re-prompt context
- If Gate 0 is not yet started `[ ]`: proceed to Step 2 (Gate 0)

**Step 2 -- Gate 0: Codebase Assessment:**
Read `references/gate-0-codebase.md` for the complete Gate 0 specification.
Follow the gate-0-codebase specification to:
1. Spawn a sub-agent to scan the codebase (20-40 files)
2. Synthesize findings into `docs/codebase-assessment.md`
3. Present findings and enter produce-then-review cycle
4. Generate review checklist (`docs/reviews/gate-0-review.md`) using `references/review-checklist-template.md`
5. Validate checklist completeness before recording approval
6. Record Gate 0 approval in `progress.txt` per `references/progress-format.md`

Proceed to Step 3.

**Step 3 -- Gate 0 Transition:**
Gate 0 is complete. Proceed to Step 4 (Gate WB Offer).

**Step 4 -- Gate WB: Working Backwards (Optional):**
Read `references/gate-wb-working-backwards.md` for the complete Gate WB specification.

Check current Gate WB state in progress.txt:
- If already `[x]` or `[-]`: skip to Step 5 (Gate 1)
- If `[ ] Pending`: re-prompt user for decision (per DD-11)
- If `[ ]` (not yet offered): offer Gate WB per the specification

Follow the gate-wb specification to handle the 3-outcome offer:
- **Yes**: run WB interview, produce `docs/working-backwards.md`, review cycle, record approval
- **Skip**: record `[-] Gate WB: Working Backwards  Skipped` in progress.txt, proceed to Step 5
- **Defer**: record `[ ] Gate WB: Working Backwards  Pending -- offered, awaiting decision` in progress.txt, proceed to Step 5

Gate WB Pending does NOT block Gate 1. Per STATE.md decision "Gate WB Pending shows gentle reminder but does NOT hard-block" (D-09 overrides PROJ-07). Proceed to Gate 1 regardless of WB state. The Pending state is tracked for `/project` to remind the user later.

Proceed to Step 5.

**Step 5 -- Gate 1: Scope Review:**
Read `references/gate-1-prd.md` for the complete Gate 1 specification.

Follow the gate-1 specification to:
1. Silently re-read `docs/codebase-assessment.md` from disk for context (DEF-16)
2. Read `docs/working-backwards.md` if it exists (D-14 -- context only, not auto-populate)
3. Conduct the 5-round interview (Scope, Inputs/Outputs, Security, Operational, Milestone Scoping)
4. Produce `prd.md` using `assets/prd-template.md`
5. Present PRD for review (produce-then-review cycle)
6. Handle partial approval if needed (DEF-12, D-09 section checklist)
7. Generate review checklist (`docs/reviews/gate-1-review.md`)
8. Validate checklist completeness
9. Record Gate 1 approval in `progress.txt`

Proceed to Step 7 (Completion Report).

**Step 6 -- Revision Mode:**
Read `references/gate-1-prd.md` for the revision mode specification.
Read existing `prd.md` from disk.
Re-read `docs/codebase-assessment.md` from disk if it exists.

Follow the revision mode specification to:
1. Ask "What changed?" -- focused interview on affected sections only
2. Revise `prd.md` with edits
3. Present for review (produce-then-review cycle)
4. Generate or update review checklist
5. Surface downstream artifact impacts without auto-resetting (DD-6)
6. Record Gate 1 re-approval with updated date

Proceed to Step 7.

**Step 7 -- Completion Report:**
Display a summary of what was produced:

```
DEFINITION COMPLETE: [Project Title]

ARTIFACTS CREATED:
- docs/codebase-assessment.md (Gate 0) [or "Skipped (greenfield)"]
- docs/working-backwards.md (Gate WB) [or "Skipped" / "Pending"]
- prd.md (Gate 1)

REVIEW CHECKLISTS:
- docs/reviews/gate-0-review.md [if applicable]
- docs/reviews/gate-wb-review.md [if applicable]
- docs/reviews/gate-1-review.md

NEXT: Run /project to see updated status, then /design for Gate 2.
```

**Error Handling section:**
- **Missing progress.txt:** Do not proceed. Tell the user to run `/project` first to bootstrap project state.
- **Gate 0 already approved:** Skip Gate 0 (it was already done in a previous session or revision).
- **Gate 1 already approved without revision intent:** Inform the user that Gate 1 is already complete. Ask if they want to revise (which enters revision mode) or check status (run /project).
- **Interrupted session:** If the conversation is interrupted mid-gate, the user can re-invoke `/define`. The skill re-reads progress.txt and resumes from the appropriate gate based on which gates are already approved.
- **Agent scan failure:** If the codebase scan agent fails or returns empty results, report the failure to the user and offer to proceed with a manual assessment (user describes the codebase) or retry.
  </action>
  <verify>
    <automated>bash -c "test -f skills/project/define/SKILL.md && grep -q 'disable-model-invocation: true' skills/project/define/SKILL.md && grep -q 'gate-0-codebase' skills/project/define/SKILL.md && grep -q 'gate-wb-working-backwards' skills/project/define/SKILL.md && grep -q 'gate-1-prd' skills/project/define/SKILL.md && wc -l < skills/project/define/SKILL.md | awk '{if (\$1 >= 150 && \$1 <= 350) print \"PASS: line count\", \$1; else print \"FAIL: line count\", \$1}'"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at skills/project/define/SKILL.md
    - Frontmatter contains "name: define"
    - Frontmatter contains "disable-model-invocation: true"
    - Contains "# /define" header
    - Contains "Step 1" through "Step 7" numbered steps
    - Step 1 contains greenfield detection with "src/" and "app/" and "lib/" directory checks
    - Step 1 contains revision mode detection with "prd.md" existence check
    - Step 1 contains Gate WB resume detection with "Pending" check
    - Step 2 references "references/gate-0-codebase.md" via Read instruction
    - Step 4 references "references/gate-wb-working-backwards.md" via Read instruction
    - Step 5 references "references/gate-1-prd.md" via Read instruction
    - Step 5 contains "codebase-assessment.md" re-read instruction (DEF-16)
    - Step 6 contains revision mode flow with "What changed" or revision focus
    - Step 7 contains completion report with artifact list
    - Contains "AskUserQuestion" at least once
    - Contains "progress.txt" reading instruction
    - Contains Rules section with "Read fresh every time" or STATE-03 reference
    - Contains Prerequisites section
    - Contains Error Handling section
    - Line count is between 150 and 350 (inclusive)
    - Does NOT reference files outside skills/project/define/ (per D-04)
  </acceptance_criteria>
  <done>SKILL.md orchestrates all 3 gates as a single session, loads each reference at the appropriate step, handles greenfield/revision/resume detection, and stays under 350 lines.</done>
</task>

</tasks>

<verification>
- SKILL.md exists with correct frontmatter (name, description, disable-model-invocation: true)
- Line count is 150-350
- All 3 gate reference files are referenced via Read instructions at appropriate steps
- Greenfield detection (DEF-01), revision mode (DEF-15), and Gate WB resume are all handled in Step 1
- Single continuous session flow: Steps 1-7 cover the full Gate 0 -> WB -> 1 sequence (DEF-14)
- bash cicd/lint-markdown.sh passes
</verification>

<success_criteria>
- /define SKILL.md is a complete, invocable skill entry point
- Flow control handles all entry states: fresh brownfield, fresh greenfield, Gate WB pending resume, revision mode
- Each gate delegates to its reference file -- SKILL.md does NOT contain gate-specific logic (scan prompts, interview questions, template structures)
- The file could be invoked as a Claude Code skill and would produce the correct behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-define-gates-0-wb-1/02-03-SUMMARY.md`
</output>
