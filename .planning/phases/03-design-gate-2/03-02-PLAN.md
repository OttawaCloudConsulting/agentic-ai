---
phase: 03-design-gate-2
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - skills/project/design/SKILL.md
autonomous: true
requirements: [DES-01, DES-02, DES-03, DES-04, DES-05, DES-06, DES-07, DES-08]
must_haves:
  truths:
    - "SKILL.md detects mode (normal vs refresh) in Step 1 before any gate execution"
    - "SKILL.md declines to run when Gate 1 is not approved (DES-01)"
    - "SKILL.md loads reference files at the step that needs them, not upfront"
    - "SKILL.md has disable-model-invocation: true in frontmatter"
    - "SKILL.md stays under 200 lines"
  artifacts:
    - path: "skills/project/design/SKILL.md"
      provides: "Flow controller for /design skill"
      contains: "disable-model-invocation: true"
      min_lines: 100
  key_links:
    - from: "skills/project/design/SKILL.md"
      to: "skills/project/design/references/gate-2-design.md"
      via: "Read at Step 2"
      pattern: "references/gate-2-design.md"
    - from: "skills/project/design/SKILL.md"
      to: "skills/project/design/references/refresh-mode.md"
      via: "Read at Step 4"
      pattern: "references/refresh-mode.md"
    - from: "skills/project/design/SKILL.md"
      to: "progress.txt"
      via: "Read for gate validation and write for gate approval"
      pattern: "progress.txt"
---

<objective>
Create the SKILL.md flow controller for the `/design` skill -- the entry point that orchestrates Gate 2 normal mode and refresh mode by loading reference files at the appropriate steps.

Purpose: SKILL.md is the single entry point for `/design`. It detects mode, validates prerequisites, and delegates to reference files for detailed gate logic -- following the same pattern as `/define`'s SKILL.md.
Output: `skills/project/design/SKILL.md` (~150 lines)
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-gate-2/03-CONTEXT.md
@.planning/phases/03-design-gate-2/03-RESEARCH.md
@.planning/phases/03-design-gate-2/03-01-SUMMARY.md

<interfaces>
<!-- Reference files created in Plan 01 that SKILL.md will load -->

gate-2-design.md: Complete Gate 2 spec loaded at Step 2. Contains:
- Input loading (prd.md, codebase-assessment.md)
- Architecture agent spawn (15-30 files, architecture-focused)
- Document synthesis using assets/architecture-template.md
- Tradeoff callouts (2-4 key decisions)
- Review phase (Approve/Revise/Partial with multiSelect)
- Checklist validation (gate-2-review.md generation)
- Gate approval recording in progress.txt

refresh-mode.md: Refresh mode spec loaded at Step 4. Contains:
- Deviation scan (milestones/*/plans/*.md glob)
- Zero-deviation early exit
- Per-deviation review with multiSelect
- Consolidation into architecture doc
- Section-by-section re-review
- Gate date update in progress.txt

progress-format.md: Progress file format (loaded by gate-2-design.md and refresh-mode.md as needed)

review-checklist-template.md: Gate 2 checklist template (loaded by gate-2-design.md during checklist validation)

From skills/project/define/SKILL.md (structural model, 197 lines):
- Frontmatter: name, description (with trigger phrases), disable-model-invocation: true
- Rules section: read fresh, produce-then-review, checklist completeness, write-ordering, interactive prompts, no auto-dispatch
- Prerequisites section
- Step 1: Detect Mode and State (all routing logic in one step)
- Steps 2-N: Gate execution (each reads its reference file)
- Completion Report step
- Error Handling section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md flow controller</name>
  <files>skills/project/design/SKILL.md</files>
  <read_first>
    skills/project/define/SKILL.md
    skills/project/design/references/gate-2-design.md
    skills/project/design/references/refresh-mode.md
    skills/project/DESIGN.md
    .planning/phases/03-design-gate-2/03-CONTEXT.md
  </read_first>
  <action>
Create `skills/project/design/SKILL.md` (~150 lines) following the exact structural pattern of `skills/project/define/SKILL.md`.

**Frontmatter** (per D-01, RESEARCH.md code example). Use YAML frontmatter delimiters with these fields:
- `name: design`
- `description: >` (multi-line) "Architecture and design specification from an approved PRD, with in-session revision before gate approval. Supports refresh mode to consolidate architectural deviations. Use when designing architecture, creating design doc, reviewing technical choices, or refreshing architecture after implementation deviations. Phrases like 'design the architecture', 'create design doc', 'refresh architecture' are good triggers."
- `disable-model-invocation: true`

**Title:** `# /design -- Architecture and Design (Gate 2)`

**Introduction paragraph:** 1-2 sentences. "Produces `docs/ARCHITECTURE_AND_DESIGN.md` from an approved PRD and optional codebase assessment, with in-session revision before gate approval. Supports refresh mode to consolidate accumulated architectural deviations from feature plans."

**Rules section** (carry forward from /define, adapted for /design):
- **Read fresh every time.** Read `progress.txt` from disk on every invocation -- never rely on conversation memory or cached values (STATE-03).
- **Produce-then-review at every gate.** Produce the full artifact, present it, offer Approve / Revise / Partial. Rejection = revision request (D-10).
- **All checklist items must be resolved.** Every review checklist item must be `[x]` or `[-]` (N/A with reason) before recording gate approval (D-11).
- **Write-ordering contract.** When writing to state files, follow STATE-04 ordering. For `/design`, only `progress.txt` is modified (gate entry).
- **Interactive prompts.** Use `AskUserQuestion` for all user-facing choices (2-4 options, max 12-character headers).
- **No auto-dispatch.** Tell the user what to run next after completion. Never auto-invoke another skill.

**Prerequisites section:**
- Working directory is the project root (where `progress.txt` lives).
- `progress.txt` must exist. If not, instruct user to run `/project` first.
- Gate 1 must be `[x]` approved in `progress.txt` (DES-01).

**Step 1 -- Detect Mode and State** (all routing in one step, per /define pattern):

Read `progress.txt` from the project root.

**Prerequisite check (DES-01):**
If Gate 1 is not `[x]` approved in `progress.txt`, inform the user:
"Gate 1 (Scope Review) must be approved before running /design. Run /define to complete the PRD first."
Do not proceed. End the session.

**Refresh mode detection (DES-08):**
Check ALL of the following:
- Gate 2 is `[x]` approved in `progress.txt`
- `docs/ARCHITECTURE_AND_DESIGN.md` exists on disk

If ALL true: jump to Step 4 (Refresh Mode).

**Already-approved detection (no refresh intent):**
If Gate 2 is `[x]` approved AND `docs/ARCHITECTURE_AND_DESIGN.md` exists AND user's message does NOT signal refresh intent (keywords: "refresh", "update architecture", "consolidate deviations", "sync deviations"):
Inform user Gate 2 is already approved. Use `AskUserQuestion` with options: **Refresh** (enters refresh mode) or **Status** (suggests running `/project`).

**Normal mode:**
If Gate 2 is not yet approved: proceed to Step 2 (Architecture Generation).

**Step 2 -- Architecture Generation:**

Read `references/gate-2-design.md` for the complete Gate 2 specification.

Follow the gate-2-design specification to:
1. Read `prd.md` and `docs/codebase-assessment.md` (if exists) (DES-02).
2. Spawn architecture sub-agent to scan 15-30 files (D-05, D-06).
3. Synthesize findings + PRD into `docs/ARCHITECTURE_AND_DESIGN.md` using `assets/architecture-template.md` (DES-03).
4. Present tradeoff callouts for 2-4 key decisions (D-09).
5. Enter produce-then-review cycle with section-by-section partial approval (DES-05, DES-06, D-08).
6. Generate and validate review checklist `docs/reviews/gate-2-review.md` (DES-04, D-11).
7. Record Gate 2 approval in `progress.txt` (DES-07).

Proceed to Step 3.

**Step 3 -- Completion Report:**

Display summary of what was produced:

```
DESIGN COMPLETE: [Project Title]

ARTIFACTS CREATED:
- docs/ARCHITECTURE_AND_DESIGN.md (Gate 2)

REVIEW CHECKLISTS:
- docs/reviews/gate-2-review.md

NEXT: Run /project to see updated status, then /milestone for Gate 3.
```

**Step 4 -- Refresh Mode:**

Read `references/refresh-mode.md` for the complete refresh mode specification.

Follow the refresh-mode specification to:
1. Scan `milestones/*/plans/*.md` for Architectural Deviations sections.
2. If zero deviations found: report "No architectural deviations found. Architecture doc is current." and exit (D-14).
3. Present each deviation with original design decision context (D-12).
4. User selects which deviations to consolidate via multiSelect.
5. Apply selected deviations to `docs/ARCHITECTURE_AND_DESIGN.md` (D-13).
6. Present updated doc for section-by-section review.
7. Update Gate 2 date in `progress.txt`.

Display refresh completion summary:

```
DESIGN REFRESHED: [Project Title]

DEVIATIONS CONSOLIDATED: N of M
ARTIFACTS UPDATED:
- docs/ARCHITECTURE_AND_DESIGN.md (refreshed)

NEXT: Run /project to see updated status.
```

**Error Handling section:**
- **Missing progress.txt:** Do not proceed. Tell user to run `/project` first.
- **Gate 1 not approved:** Do not proceed. Tell user to run `/define` first (DES-01).
- **Gate 2 already approved without refresh intent:** Inform user, offer Refresh or Status check.
- **Agent scan failure:** Report failure, offer manual architecture input (user describes system) or retry.
- **Missing prd.md:** This should not happen if Gate 1 is approved. Report inconsistency -- Gate 1 is marked approved but `prd.md` is missing. Suggest running `/project` to check state.
- **Interrupted session:** User can re-invoke `/design`. Skill re-reads `progress.txt` and resumes from appropriate state.
  </action>
  <verify>
    <automated>bash cicd/lint-markdown.sh</automated>
  </verify>
  <acceptance_criteria>
    - skills/project/design/SKILL.md exists
    - File contains frontmatter with `name: design`
    - File contains `disable-model-invocation: true`
    - File contains "## Step 1 -- Detect Mode and State"
    - File contains "## Step 2 -- Architecture Generation"
    - File contains "## Step 3 -- Completion Report"
    - File contains "## Step 4 -- Refresh Mode"
    - File contains "Gate 1 (Scope Review) must be approved" (DES-01 prerequisite check)
    - File contains "references/gate-2-design.md" (lazy-loads reference at Step 2)
    - File contains "references/refresh-mode.md" (lazy-loads reference at Step 4)
    - File contains "prd.md" and "codebase-assessment.md" (DES-02 inputs)
    - File contains "docs/ARCHITECTURE_AND_DESIGN.md" (DES-03 output)
    - File contains "docs/reviews/gate-2-review.md" (DES-04 checklist)
    - File contains "progress.txt" (DES-07 gate approval)
    - File contains "NEXT: Run /project" (no auto-dispatch)
    - File contains "## Error Handling"
    - File is between 100 and 200 lines (target ~150)
    - File contains "## Rules" section
    - File contains "## Prerequisites" section
  </acceptance_criteria>
  <done>SKILL.md is a flow controller under 200 lines that detects mode in Step 1, delegates to gate-2-design.md for normal mode and refresh-mode.md for refresh mode, handles all DES-01 through DES-08 requirements through delegation, and follows the /define SKILL.md pattern exactly.</done>
</task>

</tasks>

<verification>
1. `bash cicd/lint-markdown.sh` passes
2. SKILL.md frontmatter contains `disable-model-invocation: true`
3. SKILL.md references both `references/gate-2-design.md` and `references/refresh-mode.md`
4. SKILL.md line count is between 100-200 (target ~150)
5. Mode detection covers: prerequisite failure (DES-01), refresh mode (DES-08), already-approved, normal mode
6. No reference file is loaded before its step needs it (lazy loading pattern)
</verification>

<success_criteria>
- SKILL.md created at skills/project/design/SKILL.md
- Follows /define SKILL.md structural pattern (frontmatter, rules, prerequisites, numbered steps, error handling)
- Mode detection in Step 1 covers all entry states
- Reference files loaded lazily at the step that needs them
- Under 200 lines
- Markdown lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-gate-2/03-02-SUMMARY.md`
</output>
