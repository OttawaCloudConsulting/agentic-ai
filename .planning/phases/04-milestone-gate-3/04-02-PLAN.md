---
phase: 04-milestone-gate-3
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - skills/project/milestone/SKILL.md
autonomous: true
requirements: [MIL-01, MIL-02, MIL-03, MIL-04, MIL-05, MIL-06, MIL-07, MIL-08, MIL-09, MIL-10, MIL-11, MIL-12, MIL-13]
must_haves:
  truths:
    - "SKILL.md is under 200 lines (flow controller, not monolith)"
    - "SKILL.md delegates all gate logic to reference files loaded lazily"
    - "Mode detection routes to first invocation, subsequent invocation, or revision mode"
    - "SKILL.md references Gate 2 prerequisite check before any work"
    - "SKILL.md never writes approval marker to Gate 3 -- only in-progress marker"
  artifacts:
    - path: "skills/project/milestone/SKILL.md"
      provides: "Flow controller entry point for /milestone skill"
      min_lines: 120
      contains: "disable-model-invocation: true"
  key_links:
    - from: "skills/project/milestone/SKILL.md"
      to: "skills/project/milestone/references/gate-3-milestone.md"
      via: "lazy reference loading at Step 2/3"
      pattern: "gate-3-milestone\\.md"
    - from: "skills/project/milestone/SKILL.md"
      to: "skills/project/milestone/references/revision-mode.md"
      via: "lazy reference loading at revision step"
      pattern: "revision-mode\\.md"
---

<objective>
Create the SKILL.md flow controller for the /milestone skill (Gate 3).

Purpose: SKILL.md is the entry point that detects mode, checks prerequisites, and delegates to reference files. Following the established pattern from /define (197 lines) and /design (143 lines), it stays under 200 lines and loads references lazily.

Output: `skills/project/milestone/SKILL.md`
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-milestone-gate-3/04-CONTEXT.md
@.planning/phases/04-milestone-gate-3/04-RESEARCH.md
@.planning/phases/04-milestone-gate-3/04-01-SUMMARY.md

<interfaces>
<!-- Predecessor SKILL.md pattern to replicate. READ skills/project/design/SKILL.md for exact structure. -->

The /design SKILL.md (143 lines) has this structure:
- YAML frontmatter with name, description (multi-line with trigger phrases), disable-model-invocation: true
- Title: # /design -- Architecture and Design (Gate 2)
- 1-paragraph summary
- ## Rules (6 rules: read fresh, produce-then-review, checklist completion, write-ordering, interactive prompts, no auto-dispatch)
- ## Prerequisites (3 bullet points: project root, progress.txt exists, prior gate approved)
- ## Step 1 -- Detect Mode and State (read progress.txt, prerequisite check, mode routing with 4 branches)
- ## Step 2 -- Architecture Generation (read reference file, follow spec)
- ## Step 3 -- Completion Report (display summary with artifacts, checklists, next step)
- ## Step 4 -- Refresh Mode (read reference file, follow spec)
- ## Error Handling (4-5 error cases with resolution)

The /design frontmatter uses: name: design, description: > (multi-line), disable-model-invocation: true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md flow controller</name>
  <files>skills/project/milestone/SKILL.md</files>
  <read_first>
    skills/project/design/SKILL.md
    skills/project/define/SKILL.md
    skills/project/milestone/references/gate-3-milestone.md
    skills/project/milestone/references/revision-mode.md
    .planning/phases/04-milestone-gate-3/04-CONTEXT.md
    .planning/phases/04-milestone-gate-3/04-RESEARCH.md
  </read_first>
  <action>
Create `skills/project/milestone/SKILL.md` following the /design SKILL.md pattern. Target 150-180 lines. Structure:

**Frontmatter** -- YAML with these exact fields:
- name: milestone
- description: > (multi-line) "Milestone breakdown from approved PRD and architecture doc. Supports defining milestones one at a time and revision mode for scope changes. Use when planning milestones, breaking work into milestones, or revising an existing milestone. Phrases like 'define milestones', 'milestone planning', 'break into milestones', 'revise milestone' are good triggers."
- disable-model-invocation: true

**Title and summary:**
`# /milestone -- Milestone Planning (Gate 3)` followed by a paragraph: "Breaks an approved PRD and architecture document into milestone-scoped feature breakdowns with acceptance criteria, ordering, and sizing. Each invocation defines one milestone. Supports revision mode for scope changes to existing milestones. Gate 3 stays `[~] In progress` across invocations -- closure is handled by `/project`."

**## Rules** -- 7 rules adapted from /design:
1. Read fresh every time. Read `progress.txt` from disk on every invocation -- never rely on conversation memory (STATE-03).
2. Produce-then-review at every milestone. Produce the full milestone README, present it, offer Approve / Revise / Partial (D-01).
3. All checklist items must be resolved. Every review checklist item must be `[x]` or `[-]` (N/A with reason) before recording approval.
4. Write-ordering contract. When updating state files: write `milestone-status.txt` first, then `progress.txt` (STATE-04).
5. Interactive prompts. Use `AskUserQuestion` for all user-facing choices (2-4 options, max 12-character headers).
6. No auto-dispatch. Tell the user what to run next after completion. Never auto-invoke another skill.
7. Gate 3 stays open. /milestone NEVER writes `[x]` to Gate 3 in progress.txt -- only `[~] In progress`. Gate 3 closure is `/project`'s responsibility (D-05).

**## Prerequisites** -- 3 bullets:
- Working directory is the project root (where `progress.txt` lives).
- `progress.txt` must exist. If it does not, instruct the user to run `/project` first to bootstrap project state.
- Gate 2 must be `[x]` approved in `progress.txt` (MIL-01).

**## Step 1 -- Detect Mode and State** --
Read `progress.txt` from the project root.

Prerequisite check (MIL-01): If Gate 2 is not `[x]` approved in `progress.txt`, inform the user: "Gate 2 (Design Review) must be approved before milestone planning. Run /design to complete the architecture document first." Do not proceed. End the session.

Read `prd.md` Milestones section.

Mode detection with 4 branches:
1. **First invocation:** prd.md Milestones section is `(to be defined)` AND no `milestones/` directories exist --> proceed to Step 2
2. **Subsequent invocation:** prd.md Milestones section is populated AND some milestones lack directories --> proceed to Step 3
3. **Revision mode:** Target milestone directory already exists (D-10) --> proceed to Step 5
4. **All milestones defined:** prd.md Milestones section is populated AND all milestones have directories --> inform user all milestones are defined. Suggest running `/project` to check gate status or specify a milestone number for revision.

For branch 3 (revision mode): determine the target milestone. If user specified a milestone number or name, use that. Otherwise use `AskUserQuestion` to ask which milestone to revise.

**## Step 2 -- First Invocation: Propose Milestone Plan** --
Read `references/gate-3-milestone.md` for the complete Gate 3 specification.
Follow the "First Invocation" section of the gate-3-milestone specification to:
1. Read `prd.md` and `docs/ARCHITECTURE_AND_DESIGN.md` (MIL-02).
2. Propose full milestone plan with summaries and ordering (D-07, D-01).
3. Present tradeoff callouts (D-03).
4. Approve/Revise cycle.
5. Persist approved plan in `prd.md` Milestones section (D-08).
6. Define milestone #1 (Step 4) or allow user to defer.

**## Step 3 -- Subsequent Invocation: Select Milestone** --
Read `references/gate-3-milestone.md` for the complete Gate 3 specification.
Follow the "Subsequent Invocation" section to:
1. Auto-select next undefined milestone (D-09).
2. User can override to target a specific milestone.
3. Proceed to Step 4 (Define Milestone).

**## Step 4 -- Define and Review Milestone** --
Follow the "Define Individual Milestone" and "Review Phase" sections of gate-3-milestone.md to:
1. Generate README.md, milestone-status.txt, gate-3-review.md (MIL-03, MIL-04, MIL-05, MIL-06).
2. Update progress.txt (MIL-07, MIL-09).
3. Present for review (MIL-08).
4. Validate checklist (MIL-06).
Proceed to Step 6.

**## Step 5 -- Revision Mode** --
Read `references/revision-mode.md` for the complete revision mode specification.
Follow the revision-mode specification to:
1. Load existing milestone artifacts (MIL-10).
2. Present feature checklist for impact assessment (MIL-11, D-11).
3. Preserve completed features (MIL-12).
4. Apply focused revision (D-12).
5. Update milestone-status.txt, progress.txt, prd.md (MIL-13).
6. Generate fresh gate-3-review.md (D-13).
Proceed to Step 6.

**## Step 6 -- Completion Report** --
Display summary showing: MILESTONE DEFINED (or REVISED), ARTIFACTS CREATED (or UPDATED) listing milestones/NN-name/README.md, milestone-status.txt, and reviews/gate-3-review.md, STATE UPDATED listing progress.txt (Gate 3: [~] In progress, milestone summary line) and prd.md (Milestones section), NEXT suggesting /milestone for next milestone or /project for gate status.

For revision mode, also show FEATURES AFFECTED (N reset to pending) and FEATURES PRESERVED (M unchanged).

**## Error Handling** -- 5 cases:
- **Missing progress.txt:** Do not proceed. Tell the user to run `/project` first to bootstrap project state.
- **Gate 2 not approved:** Do not proceed. Tell the user to run `/design` first (MIL-01).
- **Missing prd.md:** Report inconsistency -- Gate 2 is marked approved but `prd.md` is missing. Suggest running `/project` to check state.
- **Missing ARCHITECTURE_AND_DESIGN.md:** Report inconsistency -- Gate 2 is marked approved but architecture doc is missing. Suggest running `/project` to check state.
- **Interrupted session:** User can re-invoke `/milestone`. Skill re-reads `progress.txt` and prd.md to detect correct mode and resume from appropriate state.
  </action>
  <verify>
    <automated>bash cicd/lint-markdown.sh && test -f skills/project/milestone/SKILL.md && wc -l skills/project/milestone/SKILL.md | awk '{if ($1 <= 200) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (max 200)"}'</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `disable-model-invocation: true` in frontmatter
    - SKILL.md contains `name: milestone` in frontmatter
    - SKILL.md contains "# /milestone -- Milestone Planning (Gate 3)"
    - SKILL.md contains "## Rules" section with 7 rules
    - SKILL.md contains "## Prerequisites" section with Gate 2 check (MIL-01)
    - SKILL.md contains "## Step 1 -- Detect Mode and State" with 4 mode branches
    - SKILL.md contains "## Step 2 -- First Invocation" referencing gate-3-milestone.md
    - SKILL.md contains "## Step 3 -- Subsequent Invocation" referencing gate-3-milestone.md
    - SKILL.md contains "## Step 4 -- Define and Review Milestone"
    - SKILL.md contains "## Step 5 -- Revision Mode" referencing revision-mode.md
    - SKILL.md contains "## Step 6 -- Completion Report" with artifact summary
    - SKILL.md contains "## Error Handling" section
    - SKILL.md contains "[~] Gate 3" (never "[x] Gate 3")
    - SKILL.md does NOT contain "[x] Gate 3"
    - SKILL.md line count is <= 200
    - SKILL.md contains "gate-3-milestone.md" (lazy reference loading)
    - SKILL.md contains "revision-mode.md" (lazy reference loading)
    - SKILL.md contains "STATE-04" or "milestone-status.txt first" (write-ordering)
  </acceptance_criteria>
  <done>SKILL.md flow controller is under 200 lines, delegates gate logic to reference files, routes correctly between first invocation, subsequent invocation, revision mode, and all-defined states, and never writes [x] to Gate 3.</done>
</task>

</tasks>

<verification>
- `skills/project/milestone/SKILL.md` exists and is under 200 lines
- `bash cicd/lint-markdown.sh` passes
- SKILL.md references `gate-3-milestone.md` and `revision-mode.md` (lazy loading)
- SKILL.md has `disable-model-invocation: true` in frontmatter
- SKILL.md never contains `[x] Gate 3`
</verification>

<success_criteria>
- SKILL.md follows the flow controller pattern from /design (frontmatter, rules, prerequisites, numbered steps, error handling)
- Mode detection covers all 4 branches (first invocation, subsequent, revision, all-defined)
- All 13 MIL requirements are addressed through delegation to reference files
- Under 200 lines total
</success_criteria>

<output>
After completion, create `.planning/phases/04-milestone-gate-3/04-02-SUMMARY.md`
</output>
