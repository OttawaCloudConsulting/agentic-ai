---
phase: 06-build
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - skills/project/build/SKILL.md
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04, BUILD-05, BUILD-06, BUILD-07, BUILD-08, BUILD-09]
must_haves:
  truths:
    - "SKILL.md is a flow controller under 200 lines that delegates all detailed logic to reference files"
    - "SKILL.md has disable-model-invocation: true in frontmatter"
    - "SKILL.md reads state files fresh on every invocation (STATE-03)"
    - "SKILL.md validates Gate 4-approved plan exists before proceeding (BUILD-01)"
    - "SKILL.md refreshes codebase assessment before reading feature plan (D-06)"
    - "SKILL.md has write-ordering contract: milestone-status.txt before progress.txt (STATE-04)"
  artifacts:
    - path: "skills/project/build/SKILL.md"
      provides: "/build flow controller entry point"
      min_lines: 130
      contains: "disable-model-invocation: true"
  key_links:
    - from: "skills/project/build/SKILL.md"
      to: "skills/project/build/references/build-execution.md"
      via: "Step 3 and Step 4 delegate to build-execution.md"
      pattern: "references/build-execution\\.md"
    - from: "skills/project/build/SKILL.md"
      to: "skills/project/build/references/codebase-refresh.md"
      via: "Step 2 delegates to codebase-refresh.md"
      pattern: "references/codebase-refresh\\.md"
    - from: "skills/project/build/SKILL.md"
      to: "skills/project/build/references/deviation-recording.md"
      via: "Referenced from sub-feature loop for deviation handling"
      pattern: "references/deviation-recording\\.md"
---

<objective>
Create the SKILL.md flow controller for the /build skill -- the entry point that orchestrates the build workflow by delegating to reference files.

Purpose: SKILL.md is the file Claude Code reads when the user invokes `/build`. It defines the step-by-step flow, rules, prerequisites, and error handling. All detailed logic lives in the reference files created in Plan 01; SKILL.md references them by path.

Output: `skills/project/build/SKILL.md` (~150-180 lines)
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-build/06-CONTEXT.md
@.planning/phases/06-build/06-RESEARCH.md
@.planning/phases/06-build/06-01-SUMMARY.md

<interfaces>
From skills/project/plan/SKILL.md (pattern template):
- Frontmatter fields: name, description (multiline), disable-model-invocation: true
- 7 rules section
- Prerequisites section
- 5 steps: Detect Mode, Load Inputs, Generate/Review, Re-plan, Completion Report
- Error Handling section
- Total: 160 lines

From skills/project/milestone/SKILL.md (dual state file write pattern):
- Write-ordering contract in Rules section: milestone-status.txt first, then progress.txt (STATE-04)
- 7 rules matching /plan pattern

Reference files this SKILL.md delegates to (created by Plan 01):
- skills/project/build/references/build-execution.md (sub-feature loop, commit, test, state updates)
- skills/project/build/references/codebase-refresh.md (incremental assessment refresh)
- skills/project/build/references/deviation-recording.md (deviation detection and recording)
- skills/project/build/references/progress-format.md (state file format spec)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /build SKILL.md flow controller</name>
  <files>skills/project/build/SKILL.md</files>
  <read_first>
    - skills/project/plan/SKILL.md (direct pattern template -- 160 lines, 7 rules, 5 steps, error handling)
    - skills/project/milestone/SKILL.md (dual state file write pattern, write-ordering contract in rules)
    - skills/project/build/references/build-execution.md (the spec this SKILL.md delegates to for Steps 3-4)
    - skills/project/build/references/codebase-refresh.md (the spec this SKILL.md delegates to for Step 2)
    - skills/project/build/references/deviation-recording.md (referenced during sub-feature loop)
    - .planning/phases/06-build/06-CONTEXT.md (all locked decisions D-01 through D-13)
    - .planning/REQUIREMENTS.md (BUILD-01 through BUILD-09)
  </read_first>
  <action>
Create `skills/project/build/SKILL.md` following the /plan SKILL.md pattern. Target ~150-180 lines. Structure:

**Frontmatter:** YAML frontmatter block with three fields:
- `name: build`
- `description:` multiline (use `>`) -- "Sub-feature-by-sub-feature implementation from Gate 4-approved plans. Refreshes codebase assessment, implements code, tracks deviations, runs tests, and updates state files. Supports multi-session builds via auto-resume. Use when implementing a feature, building code, or continuing a build session."
- `disable-model-invocation: true`

**Title and description paragraph:**
Title: `# /build -- Feature Implementation`
Description: "Implements features sub-feature by sub-feature from Gate 4-approved plans. Each sub-feature leaves the codebase in a committable state. Refreshes codebase assessment before each new feature. Runs the feature's test command on completion. Records architectural deviations when implementation diverges from the plan. Updates both `milestone-status.txt` and `progress.txt`."

**Rules section (7 rules):**
1. **Read fresh every time.** Read `progress.txt` and `milestone-status.txt` from disk on every invocation -- never rely on conversation memory (STATE-03).
2. **One commit per sub-feature.** Each completed sub-feature gets its own commit (D-03). The codebase must be committable after every sub-feature.
3. **Checklist recap after each sub-feature.** Display the full Sub-Features checklist with current `[x]`/`[ ]` marks and announce the next sub-feature (D-04).
4. **Write-ordering contract.** When updating state files: write `milestone-status.txt` FIRST, then `progress.txt` (STATE-04). Never write progress.txt without having already written milestone-status.txt.
5. **Interactive prompts.** Use `AskUserQuestion` for all user-facing choices (2-4 options, max 12-character headers).
6. **No auto-dispatch.** Tell the user what to run next after completion. Never auto-invoke another skill.
7. **Test at feature completion only.** Run the test command once after all sub-features are done (D-08). Do NOT run tests per sub-feature.

**Prerequisites:**
- Working directory is the project root (where `progress.txt` lives).
- `progress.txt` must exist. If not, instruct user to run `/project` first.
- An active milestone must exist with at least one feature at `[~] planned, awaiting build` status in its `milestone-status.txt`.

**Step 1 -- Detect State and Target Feature:**
Read `progress.txt` from project root. Find the active milestone (first at `[ ]` or `[~]` status). Read that milestone's `milestone-status.txt`. Find the target feature:
- Auto-detect: first feature at `[~] planned, awaiting build` status (D-01 auto-resume)
- User can override with explicit feature name argument
- If target feature has all sub-features `[x]` but is not yet marked complete, skip to Step 4 (Feature Completion)
- If no features at `[~] planned, awaiting build`: report "No planned features ready for build. Run /plan to plan a feature first." End session.

**Step 2 -- Refresh Codebase Assessment:**
Read `references/codebase-refresh.md` and follow the incremental refresh specification (BUILD-02, D-05, D-06, D-07). This runs BEFORE reading the feature plan. Produces a standalone commit.

**Step 3 -- Load Feature Plan and Begin Build:**
Read the feature plan from the path recorded in `milestone-status.txt`. Parse the Sub-Features checklist. Find the first unchecked `[ ]` sub-feature (D-01 auto-resume). If resuming a partially-completed build, report which sub-features are already done.

Read `references/build-execution.md` and follow the Sub-Feature Execution Loop section. For deviation handling during implementation, read `references/deviation-recording.md` (BUILD-07, D-11, D-12, D-13).

**Step 4 -- Feature Completion:**
Read `references/build-execution.md` and follow the Feature Completion section (BUILD-05, BUILD-08, BUILD-09, D-08, D-09, D-10). Runs test command, handles failures, updates both state files.

**Step 5 -- Completion Report:**
Display summary block showing: FEATURE COMPLETE with feature name, ARTIFACTS UPDATED listing feature plan path (sub-features marked [x], deviations recorded), milestone-status.txt (feature: [x] complete), progress.txt (milestone: N/M features complete), COMMITS count (N sub-feature commits + 1 assessment refresh), NEXT instruction (Build next feature, or run /project to check status). If deviations were recorded, add DEVIATIONS line with count and suggestion to run /design refresh.

**Error Handling:**
- **Missing progress.txt:** Do not proceed. Tell user to run `/project` first.
- **No active milestone:** Do not proceed. Tell user to run `/milestone` first.
- **No planned features:** Report available features and their statuses. Suggest running `/plan`.
- **Missing feature plan file:** Report inconsistency -- milestone-status.txt references a plan that doesn't exist on disk. Suggest running `/plan` to re-plan the feature.
- **Missing codebase assessment:** Warn but continue -- /build can proceed without it (see codebase-refresh.md edge cases).
- **Test command fails:** Follow failure handling in build-execution.md (D-09). Hard stop with diagnosis and options.
- **Context window filling:** Follow session continuity in build-execution.md (D-02). Mark completed work, commit, report resume instructions.
- **Interrupted session:** User re-invokes `/build`. Skill re-reads state files, auto-resumes from first unchecked sub-feature (D-01).
  </action>
  <verify>
    <automated>test -f skills/project/build/SKILL.md && grep -q "disable-model-invocation: true" skills/project/build/SKILL.md && wc -l < skills/project/build/SKILL.md | awk '{if ($1 >= 130 && $1 <= 220) print "PASS"; else print "FAIL: " $1 " lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `disable-model-invocation: true` in frontmatter
    - SKILL.md contains `name: build` in frontmatter
    - SKILL.md is between 130-220 lines (flow controller, not inlined logic)
    - SKILL.md contains 7 numbered rules in the Rules section
    - SKILL.md contains "Write-ordering contract" rule referencing STATE-04
    - SKILL.md contains "references/build-execution.md" in Step 3 and/or Step 4
    - SKILL.md contains "references/codebase-refresh.md" in Step 2
    - SKILL.md contains "references/deviation-recording.md" in Step 3
    - SKILL.md contains Step 1 through Step 5 as named sections
    - SKILL.md contains Error Handling section with at least 5 error cases
    - SKILL.md contains Prerequisites section requiring progress.txt and active milestone
    - SKILL.md does NOT inline the sub-feature loop logic (delegates to reference files)
  </acceptance_criteria>
  <done>SKILL.md exists as a flow controller under 200 lines that delegates all detailed build logic to reference files. It covers all 9 BUILD requirements via delegation and all 13 locked decisions via rules and step references. The /build skill is now invokable.</done>
</task>

</tasks>

<verification>
- skills/project/build/SKILL.md exists with correct frontmatter
- Line count is between 130-220 (not bloated with inlined logic)
- All 4 reference files are referenced by path in SKILL.md
- All 7 rules are present
- All 5 steps are present
- Error handling covers at least 5 cases
</verification>

<success_criteria>
- SKILL.md is a complete flow controller following the /plan SKILL.md pattern
- Under 200 lines (soft limit), absolutely under 220
- All build logic delegated to reference files, not inlined
- All BUILD requirements and locked decisions addressed via delegation
</success_criteria>

<output>
After completion, create `.planning/phases/06-build/06-02-SUMMARY.md`
</output>
