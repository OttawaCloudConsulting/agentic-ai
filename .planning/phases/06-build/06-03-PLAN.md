---
phase: 06-build
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - docs/skills/build.md
  - docs/SKILLS.md
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-04, BUILD-05, BUILD-06, BUILD-07, BUILD-08, BUILD-09]

must_haves:
  truths:
    - "docs/skills/build.md exists with Purpose, When to Use, When NOT to Use, Behavior, Artifacts, Skill Files, Related Skills sections"
    - "docs/SKILLS.md has a Build row in the Quick Reference table"
  artifacts:
    - path: "docs/skills/build.md"
      provides: "/build skill detail documentation"
      min_lines: 80
    - path: "docs/SKILLS.md"
      provides: "Catalog with Build entry"
      contains: "Build"
  key_links:
    - from: "docs/SKILLS.md"
      to: "docs/skills/build.md"
      via: "Quick Reference table View link"
      pattern: "skills/build\\.md"
    - from: "docs/skills/build.md"
      to: "skills/project/build/SKILL.md"
      via: "Source path reference"
      pattern: "skills/project/build"
---

<objective>
Create the detail documentation for the /build skill and add its catalog entry to SKILLS.md.

Purpose: Every skill has a detail doc at `docs/skills/<name>.md` and a row in the `docs/SKILLS.md` catalog. This follows the established pattern from all prior phases.

Output: `docs/skills/build.md` detail doc and updated `docs/SKILLS.md` with Build row
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-build/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/skills/build.md and update docs/SKILLS.md</name>
  <files>docs/skills/build.md, docs/SKILLS.md</files>
  <read_first>
    - docs/skills/plan.md (direct pattern template -- detail doc structure for prior skill)
    - docs/SKILLS.md (current catalog to add Build row to)
    - skills/project/build/SKILL.md (source of truth for behavior description)
    - skills/project/build/references/build-execution.md (for accurate behavior documentation)
    - skills/project/build/references/codebase-refresh.md (for accurate behavior documentation)
    - skills/project/build/references/deviation-recording.md (for accurate behavior documentation)
  </read_first>
  <action>
**File 1: `docs/skills/build.md`** -- Follow the exact structure from `docs/skills/plan.md`:

```markdown
# /build

**Source:** `skills/project/build/`
**Command:** `/build`
**Activation:** Manual only (`disable-model-invocation: true`) -- invoked via slash command. Not auto-triggered by conversational phrases.

## Purpose

Sub-feature-by-sub-feature implementation skill that executes Gate 4-approved feature plans. Each invocation implements one feature (or continues a partially-completed build). Refreshes codebase assessment before each new feature. Each completed sub-feature gets its own commit. Runs the feature's test command on completion. Records architectural deviations when implementation diverges from the plan. Updates both `milestone-status.txt` and `progress.txt` on feature completion. Supports multi-session builds via auto-resume from the feature plan checklist.

## When to Use

- Implementing a feature after it has been planned (Gate 4 approved)
- Continuing a partially-completed feature build (auto-resumes)
- Building the next feature in a milestone

## When NOT to Use

- When you want to check project status or get routing (use `/project`)
- When you want to define project scope or create a PRD (use `/define`)
- When you want to design system architecture (use `/design`)
- When you want to define milestones and features (use `/milestone`)
- When you want to plan a feature (use `/plan`)

## Behavior
```

Then document the numbered behavior subsections based on what SKILL.md actually does:
1. **State Detection** -- reads progress.txt, finds active milestone, reads milestone-status.txt, auto-selects first `[~] planned, awaiting build` feature
2. **Codebase Assessment Refresh** -- incremental git-diff update of docs/codebase-assessment.md, standalone commit before implementation
3. **Sub-Feature Execution** -- implements sub-features in order, one commit per sub-feature, marks [x] in plan, updates milestone-status.txt count, shows checklist recap
4. **Deviation Recording** -- detects when implementation diverges from plan, asks user to confirm, writes structured 4-field entry to plan
5. **Feature Completion** -- runs test command, hard stop on failure with Fix/Update/Exit options, updates both state files (milestone-status.txt first per STATE-04)
6. **Multi-Session Support** -- auto-resumes from first unchecked sub-feature on next invocation

Then add:
- **Artifacts** section listing: updated feature plan (sub-feature marks, deviations), updated milestone-status.txt, updated progress.txt, updated docs/codebase-assessment.md, sub-feature commits
- **Skill Files** section listing all files in skills/project/build/ with brief descriptions
- **Related Skills** section: /project (status check), /plan (creates plans /build consumes), /design (refresh mode consolidates deviations)

**File 2: Update `docs/SKILLS.md`** -- Add a Build row to the Quick Reference table. Insert it in the table maintaining the grouping pattern (the pipeline skills -- Define, Design, Milestone, Plan -- are grouped together; Build goes after Plan):

```
| Build | `/build` | Sub-feature implementation from Gate 4-approved plans with test gating and deviation tracking | [View](skills/build.md) |
```
  </action>
  <verify>
    <automated>test -f docs/skills/build.md && grep -q "Build" docs/SKILLS.md && grep -q "skills/build.md" docs/SKILLS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/skills/build.md contains "# /build" as the title
    - docs/skills/build.md contains `disable-model-invocation: true` in the Activation line
    - docs/skills/build.md contains "## Purpose" section
    - docs/skills/build.md contains "## When to Use" section
    - docs/skills/build.md contains "## When NOT to Use" section
    - docs/skills/build.md contains "## Behavior" section with numbered subsections
    - docs/skills/build.md contains "## Artifacts" section
    - docs/skills/build.md contains "## Skill Files" section referencing skills/project/build/
    - docs/skills/build.md contains "## Related Skills" section
    - docs/SKILLS.md contains a row with "Build" and "/build" and "skills/build.md"
    - docs/SKILLS.md Build row is positioned after the Plan row (pipeline skill grouping)
  </acceptance_criteria>
  <done>docs/skills/build.md exists with all required sections following the plan.md pattern. docs/SKILLS.md has a Build row in the Quick Reference table. Documentation is complete for the /build skill.</done>
</task>

</tasks>

<verification>
- docs/skills/build.md exists with all 7 standard sections
- docs/SKILLS.md contains Build row with correct link
- Detail doc accurately reflects SKILL.md behavior
</verification>

<success_criteria>
- Detail doc follows the established pattern from docs/skills/plan.md
- Catalog entry is properly positioned in the table
- All behavior sections accurately describe /build functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-build/06-03-SUMMARY.md`
</output>
