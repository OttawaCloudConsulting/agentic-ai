---
phase: 07-spike-docs
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - skills/project/spike/SKILL.md
autonomous: true
requirements: [SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04, SPIKE-05, SPIKE-06]
must_haves:
  truths:
    - "SKILL.md is a flow controller under 200 lines that loads reference files at each step"
    - "SKILL.md detects new spike vs follow-up mode by checking existing spike file on disk"
    - "SKILL.md spawns research agent first, then red-team agent sequentially per D-01"
    - "SKILL.md assembles the final spike artifact from both agent outputs"
    - "SKILL.md writes spike entry to progress.txt on creation and marks resolved on user signal"
  artifacts:
    - path: "skills/project/spike/SKILL.md"
      provides: "Spike skill flow controller"
      contains: "disable-model-invocation: true"
      min_lines: 100
  key_links:
    - from: "skills/project/spike/SKILL.md"
      to: "skills/project/spike/references/research-agent.md"
      via: "Read references/research-agent.md"
      pattern: "references/research-agent"
    - from: "skills/project/spike/SKILL.md"
      to: "skills/project/spike/references/redteam-agent.md"
      via: "Read references/redteam-agent.md"
      pattern: "references/redteam-agent"
    - from: "skills/project/spike/SKILL.md"
      to: "skills/project/spike/references/spike-format.md"
      via: "Read references/spike-format.md"
      pattern: "references/spike-format"
    - from: "skills/project/spike/SKILL.md"
      to: "progress.txt"
      via: "Reads and writes spike entries"
      pattern: "progress.txt"
---

<objective>
Create the SKILL.md flow controller for the /spike skill that orchestrates the research and red-team agents, assembles spike artifacts, and manages spike lifecycle in progress.txt.

Purpose: SKILL.md is the entry point invoked by `/spike`. It loads reference files at each step, spawns agents sequentially, and manages state -- following the established flow controller pattern from /build, /design, etc.
Output: skills/project/spike/SKILL.md (~150-180 lines)
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-spike-docs/07-CONTEXT.md
@.planning/phases/07-spike-docs/07-RESEARCH.md
@.planning/phases/07-spike-docs/07-01-SUMMARY.md

<interfaces>
<!-- Reference files created by Plan 01 that SKILL.md loads -->
From skills/project/spike/references/research-agent.md:
- Loaded at Step 2 for research agent spawning
- Defines Agent Prompt, Agent Tool Access, output path /tmp/spike-research-findings.md

From skills/project/spike/references/redteam-agent.md:
- Loaded at Step 3 for red-team agent spawning
- Defines adversarial Agent Prompt, Agent Tool Access, output path /tmp/spike-redteam-findings.md

From skills/project/spike/references/spike-format.md:
- Loaded at Step 4 for artifact assembly
- Defines Topic Slug Generation, New Spike Template (8 sections), Follow-Up Log Entry Format, Assembly Instructions, Resolution

From skills/project/spike/references/progress-format.md:
- Loaded when writing spike entries to progress.txt
- Defines Spikes Section Format: `[ ] Spike Name  docs/spikes/slug.md` and `[x] Spike Name  docs/spikes/slug.md  Resolved: YYYY-MM-DD`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spike SKILL.md flow controller</name>
  <files>skills/project/spike/SKILL.md</files>
  <read_first>
    - skills/project/build/SKILL.md (primary template -- flow controller pattern, ~144 lines, frontmatter, rules, prerequisites, numbered steps, error handling, completion report)
    - skills/project/design/SKILL.md (sub-agent spawning pattern -- Step 2 architecture agent, mode detection pattern)
    - skills/project/spike/references/research-agent.md (created by Plan 01 -- understand what SKILL.md loads)
    - skills/project/spike/references/redteam-agent.md (created by Plan 01 -- understand what SKILL.md loads)
    - skills/project/spike/references/spike-format.md (created by Plan 01 -- understand artifact assembly)
    - skills/project/spike/references/progress-format.md (created by Plan 01 -- spike entry format)
    - .planning/phases/07-spike-docs/07-CONTEXT.md (all locked decisions D-01 through D-12)
    - .planning/phases/07-spike-docs/07-RESEARCH.md (architecture patterns, pitfalls)
  </read_first>
  <action>
Create `skills/project/spike/SKILL.md` following the /build SKILL.md pattern. Target: 150-180 lines. Structure:

**Frontmatter** (use the exact YAML frontmatter from RESEARCH.md code example -- the frontmatter block with `name: spike`, multiline `description` using `>` folded scalar describing adversarial technical research with red-team validation, and `disable-model-invocation: true`):

Key frontmatter fields:
- name: spike
- description: Adversarial technical research with red-team validation. Spawns research and red-team sub-agents to investigate technical questions and produce structured spike artifacts. Supports follow-up research on existing spikes. Use when investigating technical feasibility, comparing approaches, or validating assumptions before committing to a plan.
- disable-model-invocation: true

**Title and overview paragraph:** `# /spike -- Adversarial Technical Research`
Brief description: Accepts a research question and available tooling list, spawns sequential research and red-team agents, produces a structured spike artifact. Supports follow-up research and lifecycle tracking in progress.txt.

**## Rules** (follow /build pattern):
1. Read fresh every time. Read `progress.txt` from disk on every invocation -- never rely on conversation memory (STATE-03).
2. Sequential agents per D-01. Research agent completes first, then red-team agent reviews. Never run concurrently.
3. Final artifact only per D-02. Both agents run autonomously without user checkpoints. User sees the completed spike doc.
4. Write-ordering contract. When updating state files, follow STATE-04 ordering.
5. Interactive prompts. Use `AskUserQuestion` for all user-facing choices (2-4 options, max 12-character headers).
6. No auto-dispatch. Tell the user what to run next after completion. Never auto-invoke another skill.

**## Prerequisites:**
- Working directory is the project root (where `progress.txt` lives).
- `progress.txt` must exist. If not, instruct user to run `/project` first. Per D-06, requires bootstrapped project.

**## Step 1 -- Gather Input and Detect Mode:**
Read `progress.txt` from the project root.

Gather the user's research question and available tooling list per D-05/SPIKE-01. If the user provided these in their invocation message, use them directly. If not provided, use `AskUserQuestion` to ask for:
- Research question (what technical question to investigate)
- Available tooling (libraries, tools, approaches to evaluate -- can be empty)

**Follow-up mode detection (SPIKE-05):**
Read `references/spike-format.md` for the Topic Slug Generation rules. Generate the slug from the topic. Check if `docs/spikes/{slug}.md` exists on disk.
- If file exists: enter follow-up mode (Step 5).
- If file does not exist: enter new spike mode (Step 2).
- If slug collision (file exists but different question): use `AskUserQuestion` with options: **Follow-up** (append to existing spike) or **New spike** (user provides a different name).

**## Step 2 -- Research Agent:**
Read `references/research-agent.md` for the complete research agent specification.

Spawn a sub-agent using the `Agent` tool following the research agent spec. Pass the research question and available tooling list to the agent prompt. The agent writes findings to `/tmp/spike-research-findings.md`.

After the agent completes, read `/tmp/spike-research-findings.md` to verify it was produced. If missing, report error and offer retry or manual input.

**## Step 3 -- Red-Team Agent:**
Read `references/redteam-agent.md` for the complete red-team agent specification.

Spawn a sub-agent using the `Agent` tool following the red-team agent spec. The agent reads `/tmp/spike-research-findings.md` (research output) and writes critique to `/tmp/spike-redteam-findings.md`.

After the agent completes, read `/tmp/spike-redteam-findings.md` to verify it was produced. If missing, report error and offer retry.

**## Step 4 -- Assemble Spike Artifact:**
Read `references/spike-format.md` for the artifact template and assembly instructions.

Follow the assembly instructions to:
1. Create `docs/spikes/` directory if it does not exist.
2. Build the spike artifact using the New Spike Template.
3. Populate sections from agent outputs: Methodology and Findings from research findings, Red-Team Assessment from red-team findings (per D-10: equal peer section, never merge or suppress).
4. Write the Recommendation section: state the recommended approach, why, remaining risks after red-team review (per D-11).
5. Set Status to "open".
6. Set Follow-Up Log to "(no follow-ups yet)".
7. Write the artifact to `docs/spikes/{slug}.md`.

**Update progress.txt (SPIKE-04, D-07):**
Read `references/progress-format.md` for spike entry format. Add a `[ ]` entry under `## Spikes` in `progress.txt`. If `(none yet)` placeholder exists, replace it with the entry. Format: `[ ] {Spike Name}  docs/spikes/{slug}.md`

Proceed to Step 6 (Completion Report).

**## Step 5 -- Follow-Up Mode (SPIKE-05):**
Read `references/spike-format.md` for the Follow-Up Log Entry Format.

The user provides a follow-up question. Re-run the research and red-team agents (Steps 2-3) with the follow-up question as the research question and the same available tooling from the original spike.

After both agents complete:
1. Read the existing spike artifact from `docs/spikes/{slug}.md`.
2. Append a new Follow-Up Log entry per the dated entry format (per D-08, D-12).
3. If "(no follow-ups yet)" placeholder exists, replace it with the entry.
4. Preserve all original content -- never overwrite findings, red-team assessment, or prior follow-ups.
5. Write the updated artifact back to `docs/spikes/{slug}.md`.

**Offer resolution (SPIKE-06, D-07, D-08):**
Use `AskUserQuestion` with options: **Resolved** (mark spike resolved) / **Follow-up** (continue with another question) / **Done** (leave open, end session).

If "Resolved": Change `## Status` in the spike artifact from "open" to "resolved". Update `progress.txt` spike entry from `[ ]` to `[x]` with `Resolved: YYYY-MM-DD` suffix.
If "Follow-up": Gather the new follow-up question, repeat Step 5.
If "Done": Leave spike open, proceed to completion report.

**## Step 6 -- Completion Report:**
Display summary showing: spike name, artifact path (docs/spikes/{slug}.md), state update (progress.txt entry added/updated), and next action suggestion (review artifact or run /project for status).

If follow-ups were recorded, show count. If resolved, show resolution date.

**## Error Handling** (follow /build pattern):
- Missing progress.txt: Do not proceed. Tell user to run `/project` first.
- Research agent failure: Report failure. Offer retry or manual research input (user provides findings directly).
- Red-team agent failure: Report failure. Offer retry or skip red-team (note in artifact that red-team was not performed).
- Missing docs/spikes/ directory: Create it automatically.
- Spike artifact write failure: Report error, display artifact content so user can save manually.
- Interrupted session: User can re-invoke `/spike`. For new spikes, if artifact already written, detects as follow-up mode. For follow-ups in progress, user re-invokes and adds the follow-up.
  </action>
  <verify>
    <automated>test -f skills/project/spike/SKILL.md && grep -q "disable-model-invocation: true" skills/project/spike/SKILL.md && grep -q "references/research-agent" skills/project/spike/SKILL.md && grep -q "references/redteam-agent" skills/project/spike/SKILL.md && grep -q "references/spike-format" skills/project/spike/SKILL.md && grep -q "references/progress-format" skills/project/spike/SKILL.md && grep -q "progress.txt" skills/project/spike/SKILL.md && LINES=$(wc -l < skills/project/spike/SKILL.md) && test "$LINES" -le 200 && echo "PASS ($LINES lines)" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md exists at skills/project/spike/SKILL.md
    - Frontmatter contains `name: spike`
    - Frontmatter contains `disable-model-invocation: true`
    - File contains "## Rules" section with 6 rules
    - File contains "## Prerequisites" section requiring progress.txt
    - File contains "## Step 1" for input gathering and mode detection
    - File contains "## Step 2" referencing `references/research-agent.md`
    - File contains "## Step 3" referencing `references/redteam-agent.md`
    - File contains "## Step 4" referencing `references/spike-format.md` for assembly
    - File contains "## Step 5" for follow-up mode with append behavior
    - File contains "## Step 6" for completion report
    - File contains "## Error Handling" section
    - File references all four reference files: research-agent.md, redteam-agent.md, spike-format.md, progress-format.md
    - File is under 200 lines (target 150-180)
    - File contains `AskUserQuestion` for interactive prompts
    - File contains resolution flow with Resolved/Follow-up/Done options
  </acceptance_criteria>
  <done>SKILL.md exists as a flow controller under 200 lines. It loads all four reference files at appropriate steps, spawns research then red-team agents sequentially, assembles the spike artifact, manages progress.txt entries, and supports follow-up mode with resolution signaling.</done>
</task>

</tasks>

<verification>
SKILL.md is a concise flow controller that:
1. Has disable-model-invocation: true in frontmatter
2. Loads reference files at each step (not inlining logic)
3. Spawns research agent (Step 2) then red-team agent (Step 3) sequentially
4. Assembles spike artifact (Step 4) with Red-Team Assessment as equal peer section
5. Supports follow-up mode (Step 5) with append to Follow-Up Log
6. Manages progress.txt spike entries (add on creation, resolve on user signal)
7. Stays under 200 lines
</verification>

<success_criteria>
- SKILL.md exists at skills/project/spike/SKILL.md with disable-model-invocation: true
- File is under 200 lines, following /build SKILL.md pattern
- All four reference files are loaded via Read at appropriate steps
- Sequential agent flow: research agent -> red-team agent per D-01
- Follow-up mode detects existing spike file and appends per SPIKE-05
- Resolution flow uses AskUserQuestion with Resolved/Follow-up/Done per SPIKE-06
</success_criteria>

<output>
After completion, create `.planning/phases/07-spike-docs/07-02-SUMMARY.md`
</output>
