---
phase: 07-spike-docs
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - docs/skills/spike.md
  - docs/SKILLS.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "docs/skills/spike.md detail doc exists with Purpose, When to Use, When NOT to Use, Behavior, Artifacts, Skill Files, Related Skills sections"
    - "docs/SKILLS.md has a Spike row in the catalog table"
    - "All 7 SKILL.md files have disable-model-invocation: true in frontmatter"
  artifacts:
    - path: "docs/skills/spike.md"
      provides: "Spike skill detail documentation"
      contains: "## Purpose"
    - path: "docs/SKILLS.md"
      provides: "Updated catalog with /spike row"
      contains: "/spike"
  key_links:
    - from: "docs/SKILLS.md"
      to: "docs/skills/spike.md"
      via: "Catalog row links to detail doc"
      pattern: "skills/spike.md"
    - from: "docs/skills/spike.md"
      to: "skills/project/spike/"
      via: "Source path reference"
      pattern: "skills/project/spike"
---

<objective>
Create the /spike detail doc, add the /spike catalog row to SKILLS.md, and verify all 7 skills pass the DOCS-01/02/03 compliance audit.

Purpose: Complete suite documentation so every skill has a detail doc, catalog entry, and SKILL.md with disable-model-invocation: true. This is the final deliverable for the v1 skill suite.
Output: docs/skills/spike.md (new), docs/SKILLS.md (updated), verification that all 7 skills are compliant
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-spike-docs/07-CONTEXT.md
@.planning/phases/07-spike-docs/07-RESEARCH.md
@.planning/phases/07-spike-docs/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: SKILL.md created at skills/project/spike/SKILL.md -->
<!-- From Plan 01: Reference files at skills/project/spike/references/ -->
<!-- Existing detail doc pattern from docs/skills/build.md -->
<!-- Existing catalog pattern from docs/SKILLS.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/skills/spike.md detail doc and update SKILLS.md catalog</name>
  <files>docs/skills/spike.md, docs/SKILLS.md</files>
  <read_first>
    - docs/skills/build.md (template -- most recent detail doc, follows Purpose/When to Use/When NOT to Use/Behavior/Artifacts/Skill Files/Related Skills pattern per D-15)
    - docs/SKILLS.md (catalog table -- need to add /spike row after /plan row, following pipeline grouping per Phase 06 decision)
    - skills/project/spike/SKILL.md (created by Plan 02 -- source for behavior description, steps, artifacts)
    - skills/project/spike/references/spike-format.md (created by Plan 01 -- artifact sections for documentation)
    - skills/project/spike/references/research-agent.md (created by Plan 01 -- for behavior description)
    - skills/project/spike/references/redteam-agent.md (created by Plan 01 -- for behavior description)
    - .planning/phases/07-spike-docs/07-CONTEXT.md (D-13, D-14, D-15 for documentation decisions)
  </read_first>
  <action>
**Create docs/skills/spike.md** following the docs/skills/build.md structure exactly per D-15:

```markdown
# /spike

**Source:** `skills/project/spike/`
**Command:** `/spike`
**Activation:** Manual only (`disable-model-invocation: true`) -- invoked via slash command. Not auto-triggered by conversational phrases.

## Purpose

Adversarial technical research skill that investigates technical questions using sequential research and red-team agents. Accepts a research question and list of available tooling, produces a structured spike artifact at `docs/spikes/<topic>.md` with both research findings and independent red-team validation. The two perspectives are presented as equal peer sections -- never merged or suppressed. Supports follow-up research on existing spikes with dated append entries. Tracks spike lifecycle in `progress.txt` (open/resolved).

## When to Use

- Investigating technical feasibility before committing to an approach
- Comparing libraries, frameworks, or tools for a specific use case
- Validating technical assumptions with adversarial review
- Researching integration approaches for external services
- Any technical question that benefits from a devil's advocate perspective

## When NOT to Use

- When you want to check project status or get routing (use `/project`)
- When you want to define project scope or create a PRD (use `/define`)
- When you want to design system architecture (use `/design`)
- When you want to define milestones and features (use `/milestone`)
- When you want to plan a feature (use `/plan`)
- When you want to implement a feature (use `/build`)

## Behavior

### 1. Input Gathering and Mode Detection

Reads `progress.txt` from the project root. Gathers the user's research question and available tooling list. Generates a topic slug (kebab-case) and checks if `docs/spikes/<slug>.md` exists. If it exists, enters follow-up mode. If not, enters new spike mode.

### 2. Research Agent

Spawns a sub-agent to investigate the research question. The agent uses web search, codebase tools, and documentation to research each tool in the available tooling list. Writes structured findings to a temporary file including methodology, per-tool analysis, and comparison matrix.

### 3. Red-Team Agent

After the research agent completes, spawns a second sub-agent with an explicitly adversarial posture. The red-team agent reads the research findings and independently verifies claims using its own tool access. Challenges factual errors, missing alternatives, flawed reasoning, unverified assumptions, and version/compatibility issues. Writes structured critique to a temporary file.

### 4. Artifact Assembly

Assembles the final spike artifact from both agent outputs. The artifact has 8 fixed sections: Question, Available Tooling, Methodology, Findings, Red-Team Assessment, Recommendation, Status, Follow-Up Log. The Red-Team Assessment is an equal peer section to Findings -- the two perspectives are never merged. The Recommendation states a clear pick with rationale, incorporating red-team feedback and noting remaining risks. Adds a `[ ]` entry to progress.txt under the Spikes section.

### 5. Follow-Up Mode

When invoked on an existing spike topic, re-runs the research and red-team agents on the follow-up question. Appends a dated entry to the Follow-Up Log with full findings, red-team assessment, and updated recommendation. Preserves all original content. After each follow-up, offers resolution: Resolved (marks spike `[x]` in progress.txt), Follow-up (continues), or Done (leaves open).

### 6. Resolution

When the user signals resolution, updates the spike artifact status from "open" to "resolved" and marks the progress.txt entry `[x]` with a resolution date.

## Artifacts

| Artifact | Path | Created By |
|----------|------|------------|
| Spike artifact | `docs/spikes/<topic>.md` | /spike (new spike or follow-up append) |
| progress.txt | `progress.txt` | /spike (adds spike entry, marks resolved) |

## Skill Files

```
skills/project/spike/
+-- SKILL.md                              # Flow controller (~150-180 lines)
+-- references/
    +-- research-agent.md                 # Research sub-agent prompt and methodology spec
    +-- redteam-agent.md                  # Red-team sub-agent prompt and challenge spec
    +-- spike-format.md                   # Spike artifact template and section spec
    +-- progress-format.md                # State file format spec (own copy per D-04)
```

## Related Skills

| Skill | Relationship |
|-------|-------------|
| `/project` | Reads `progress.txt` for spike status; routes users to `/spike` |
| `/milestone` | Reads spike artifacts when referenced in milestone planning |
| `/design` | Spike findings may inform architecture decisions |
| `/plan` | Spike findings may inform feature planning |
```

**Update docs/SKILLS.md** -- Add the /spike row to the Quick Reference table. Insert it after the Build row and before the OCC Skill Creator row (following pipeline grouping: Define, Design, Milestone, Plan, Build, Spike). The row format:

```
| Spike | `/spike` | Adversarial technical research with red-team validation and follow-up support | [View](skills/spike.md) |
```

Also add `/spike` to the Consuming Skills section copying example:
```bash
cp -r skills/project/spike/               <target-repo>/.claude/skills/project/spike/
```
Insert this line after the `skills/project/plan/` line.
  </action>
  <verify>
    <automated>test -f docs/skills/spike.md && grep -q "## Purpose" docs/skills/spike.md && grep -q "## When to Use" docs/skills/spike.md && grep -q "## When NOT to Use" docs/skills/spike.md && grep -q "## Behavior" docs/skills/spike.md && grep -q "## Artifacts" docs/skills/spike.md && grep -q "## Skill Files" docs/skills/spike.md && grep -q "## Related Skills" docs/skills/spike.md && grep -q "/spike" docs/SKILLS.md && grep -q "skills/spike.md" docs/SKILLS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/skills/spike.md exists with all 7 standard sections: Purpose, When to Use, When NOT to Use, Behavior, Artifacts, Skill Files, Related Skills
    - docs/skills/spike.md contains "Source:" referencing "skills/project/spike/"
    - docs/skills/spike.md contains "disable-model-invocation: true" in the Activation line
    - docs/skills/spike.md Behavior section has 6 subsections (Input Gathering, Research Agent, Red-Team Agent, Artifact Assembly, Follow-Up Mode, Resolution)
    - docs/SKILLS.md contains a row with "/spike" in the Quick Reference table
    - docs/SKILLS.md contains "skills/spike.md" as the View link in the spike row
    - docs/SKILLS.md Consuming Skills section contains "skills/project/spike/"
  </acceptance_criteria>
  <done>docs/skills/spike.md detail doc exists with all standard sections describing the adversarial research skill. docs/SKILLS.md catalog has the /spike row in pipeline order after /build.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DOCS-01/02/03 compliance for all 7 skills</name>
  <files>docs/skills/spike.md, docs/SKILLS.md</files>
  <read_first>
    - docs/SKILLS.md (verify all 7 skill rows exist in catalog)
    - docs/skills/project.md (verify exists)
    - docs/skills/define.md (verify exists)
    - docs/skills/design.md (verify exists)
    - docs/skills/milestone.md (verify exists)
    - docs/skills/plan.md (verify exists)
    - docs/skills/build.md (verify exists)
    - docs/skills/spike.md (verify exists -- just created in Task 1)
    - skills/project/SKILL.md (verify frontmatter has disable-model-invocation: true)
    - skills/project/define/SKILL.md (verify frontmatter)
    - skills/project/design/SKILL.md (verify frontmatter)
    - skills/project/milestone/SKILL.md (verify frontmatter)
    - skills/project/plan/SKILL.md (verify frontmatter)
    - skills/project/build/SKILL.md (verify frontmatter)
    - skills/project/spike/SKILL.md (verify frontmatter -- created by Plan 02)
  </read_first>
  <action>
Run a comprehensive audit of DOCS-01, DOCS-02, and DOCS-03 for all 7 skills. This is verification, not creation -- if any gaps are found, fix them.

**DOCS-01 audit** -- Each skill has a detail doc at `docs/skills/<name>.md`:
Check that all 7 files exist:
- docs/skills/project.md
- docs/skills/define.md
- docs/skills/design.md
- docs/skills/milestone.md
- docs/skills/plan.md
- docs/skills/build.md
- docs/skills/spike.md

For each file, verify it contains at minimum: `## Purpose`, `## Behavior`, `## Artifacts`, `## Skill Files`.

**DOCS-02 audit** -- `docs/SKILLS.md` catalog has a row for each skill:
Verify the Quick Reference table contains rows for all 7 pipeline skills:
- `/project`
- `/define`
- `/design`
- `/milestone`
- `/plan`
- `/build`
- `/spike`

Each row must have a `[View](skills/<name>.md)` link.

**DOCS-03 audit** -- Each skill directory has SKILL.md with `disable-model-invocation: true`:
Verify all 7 SKILL.md files exist and contain the frontmatter key:
- skills/project/SKILL.md
- skills/project/define/SKILL.md
- skills/project/design/SKILL.md
- skills/project/milestone/SKILL.md
- skills/project/plan/SKILL.md
- skills/project/build/SKILL.md
- skills/project/spike/SKILL.md

Run: `grep -l 'disable-model-invocation: true' skills/project/SKILL.md skills/project/*/SKILL.md`
Expected: 7 files listed.

**If any gaps found:** Fix them. For DOCS-01 gaps (unlikely -- all 6 pre-existing docs verified in research), add missing sections. For DOCS-02 gaps, add missing rows to SKILLS.md. For DOCS-03 gaps, add the frontmatter key.

**Report audit results** as a checklist:
```
DOCS AUDIT COMPLETE:

DOCS-01 (detail docs): 7/7 verified
- [x] project.md  [x] define.md  [x] design.md  [x] milestone.md
- [x] plan.md  [x] build.md  [x] spike.md

DOCS-02 (catalog rows): 7/7 verified
- [x] /project  [x] /define  [x] /design  [x] /milestone
- [x] /plan  [x] /build  [x] /spike

DOCS-03 (SKILL.md frontmatter): 7/7 verified
- [x] project/SKILL.md  [x] define/SKILL.md  [x] design/SKILL.md
- [x] milestone/SKILL.md  [x] plan/SKILL.md  [x] build/SKILL.md
- [x] spike/SKILL.md
```
  </action>
  <verify>
    <automated>DETAIL=$(ls docs/skills/project.md docs/skills/define.md docs/skills/design.md docs/skills/milestone.md docs/skills/plan.md docs/skills/build.md docs/skills/spike.md 2>&1 | grep -c "\.md$") && CATALOG=$(grep -c '/project\|/define\|/design\|/milestone\|/plan\|/build\|/spike' docs/SKILLS.md) && FRONTMATTER=$(grep -rl 'disable-model-invocation: true' skills/project/SKILL.md skills/project/*/SKILL.md 2>/dev/null | wc -l | tr -d ' ') && echo "Detail docs: $DETAIL/7, Catalog rows: $CATALOG (expect >=7), Frontmatter: $FRONTMATTER/7" && test "$DETAIL" -eq 7 && test "$FRONTMATTER" -eq 7 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - 7 detail doc files exist at docs/skills/{project,define,design,milestone,plan,build,spike}.md
    - Each detail doc contains "## Purpose" and "## Behavior" sections
    - docs/SKILLS.md contains rows for all 7 pipeline skills (/project, /define, /design, /milestone, /plan, /build, /spike)
    - Each SKILLS.md row has a [View](skills/<name>.md) link
    - 7 SKILL.md files have `disable-model-invocation: true` in frontmatter (grep returns 7 results)
    - Audit report checklist shows 7/7 for all three DOCS requirements
  </acceptance_criteria>
  <done>All 7 skills pass DOCS-01 (detail doc exists), DOCS-02 (catalog row exists), and DOCS-03 (SKILL.md has disable-model-invocation: true). Full audit report produced with 7/7 across all three requirements.</done>
</task>

</tasks>

<verification>
Complete documentation suite verification:
1. docs/skills/spike.md exists with standard structure per D-15
2. docs/SKILLS.md has /spike row in pipeline order
3. All 7 skills pass DOCS-01/02/03 audit (detail doc + catalog row + SKILL.md frontmatter)
</verification>

<success_criteria>
- docs/skills/spike.md exists with all 7 standard sections
- docs/SKILLS.md contains /spike row with View link
- 7/7 detail docs verified (DOCS-01)
- 7/7 catalog rows verified (DOCS-02)
- 7/7 SKILL.md frontmatter verified (DOCS-03)
</success_criteria>

<output>
After completion, create `.planning/phases/07-spike-docs/07-03-SUMMARY.md`
</output>
