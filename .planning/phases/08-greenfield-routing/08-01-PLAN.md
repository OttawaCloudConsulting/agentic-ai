---
phase: 08-greenfield-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/project/references/routing-logic.md
  - skills/project/SKILL.md
autonomous: true
requirements:
  - PROJ-03
  - PROJ-06

must_haves:
  truths:
    - "routing-logic.md Notes section contains an explicit equivalence note stating that Gate 0 `[-]` (skipped) is treated as equivalent to `[x]` (approved) for all downstream routing"
    - "routing-logic.md Notes section mentions Gate 0 routing scope so the note is unambiguous about which `[-]` it applies to"
    - "SKILL.md Step 5 Gate WB offer condition mentions both `[x]` and `[-]` (greenfield) as triggers"
    - "SKILL.md Step 5 Gate WB offer condition references the word 'greenfield' so the context is clear"
    - "Second /project invocation on a greenfield project (Gate 0 = `[-]`) will route through the Gate WB offer condition rather than falling through to an unmatched state"
  artifacts:
    - path: "skills/project/references/routing-logic.md"
      provides: "Routing decision table with Notes-section equivalence note for Gate 0 [-] state"
      contains: "equivalent to"
    - path: "skills/project/SKILL.md"
      provides: "SKILL.md flow controller with updated Step 5 Gate WB offer condition"
      contains: "[-]"
  key_links:
    - from: "skills/project/SKILL.md Step 5"
      to: "skills/project/references/routing-logic.md Notes section"
      via: "SKILL.md reads routing-logic.md; equivalence note reinforces the updated offer condition"
      pattern: "routing-logic\\.md"
---

<objective>
Fix the greenfield routing ambiguity that causes the second `/project` invocation on a greenfield project to hit an unmatched routing state and never fire the Gate WB offer.

Purpose: Close the `GREENFIELD-ROUTING` and `GREENFIELD-E2E` integration gaps identified in the v1.0 audit. Gate 0 `[-]` (greenfield skip) has no explicit routing coverage and the Gate WB offer condition only checks `[x]`, silently skipping greenfield users.

Output: Two modified Markdown files — `routing-logic.md` with an equivalence note appended to its Notes section, and `SKILL.md` with its Step 5 Gate WB offer condition updated to include `[-]` as a trigger.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-greenfield-routing/08-CONTEXT.md
@.planning/phases/08-greenfield-routing/08-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Current state of the two files being modified. Executor must not rely on memory — read the files. -->

From skills/project/references/routing-logic.md (lines 29–35) — the Notes block the equivalence note appends to:
```markdown
Notes:

- The "Alternatives" column implements D-04 -- only actions valid for the current state appear. Do not show `/plan` if no milestone exists. Do not show `/build` if no planned features exist.
- "Bootstrap" is an internal action performed by `/project` itself -- it does not route to another skill.
- "Offer Gate WB" is also handled internally by `/project` via `AskUserQuestion`.
- When open spikes exist, `/spike` is added to the alternatives list for any state where it is contextually relevant (post-Gate 1 states).
- The "customer outcome unclear" condition for Gate WB offer is evaluated by Claude based on available project context -- if the project's purpose and target customer are well-defined from existing artifacts, Gate WB may not be offered.
```

From skills/project/SKILL.md (lines 129–131) — the Step 5 Gate WB offer condition being updated:
```markdown
**Gate WB offer (DD-11, D-08):** If Gate 0 is approved, no `docs/working-backwards.md`
exists, Gate WB has not been offered yet, and the customer outcome is unclear -- offer
Gate WB using `AskUserQuestion` with options:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gate 0 [-] equivalence note to routing-logic.md Notes section</name>
  <files>skills/project/references/routing-logic.md</files>
  <read_first>
    - skills/project/references/routing-logic.md — read the full file to see the current Notes block and confirm the exact line to append after (the last bullet in the Notes section, currently ending with "Gate WB may not be offered.")
  </read_first>
  <action>
    Append one new bullet to the end of the Notes section in `skills/project/references/routing-logic.md`.

    The Notes section currently ends after this bullet (per D-03 — note goes in Notes, not as a new table row):
    ```
    - The "customer outcome unclear" condition for Gate WB offer is evaluated by Claude based on available project context -- if the project's purpose and target customer are well-defined from existing artifacts, Gate WB may not be offered.
    ```

    Append this bullet immediately after it (per D-01, D-03, and CONTEXT.md specifics):
    ```
    - `[-]` (skipped) is treated as equivalent to `[x]` (approved) for all Gate 0 routing. Greenfield projects skip Gate 0 (codebase alignment is not applicable), but the skip counts as Gate 0 resolved for all downstream routing purposes.
    ```

    Do NOT:
    - Add a new row to the routing table — D-03 explicitly rejects this approach
    - Edit any existing routing table rows inline (cosmetic `[-]` additions are out of scope per RESEARCH.md pitfall 3)
    - Edit the Gate WB Offer Logic table (lines 74–80) — that table describes post-offer states and is NOT broken
    - Touch any other section of the file
  </action>
  <verify>
    <automated>cd /Users/christian/git-repos/OCC-github/agentic-ai && grep -q "equivalent to" skills/project/references/routing-logic.md && grep -q "Gate 0 routing\|Gate 0 resolved" skills/project/references/routing-logic.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "equivalent to" skills/project/references/routing-logic.md` exits 0
    - `grep -q "Gate 0 routing\|Gate 0 resolved" skills/project/references/routing-logic.md` exits 0
    - `grep -q "codebase alignment is not applicable" skills/project/references/routing-logic.md` exits 0
    - The Notes section contains exactly 6 bullets (5 original + 1 new)
    - The routing table rows (lines 11–27) are unchanged — `grep -c "^|" skills/project/references/routing-logic.md` returns the same count as before the edit (15 pipe-delimited rows including header and separator)
  </acceptance_criteria>
  <done>routing-logic.md Notes section contains the equivalence note; Gate 0 `[-]` is documented as equivalent to `[x]` for all downstream routing; routing table rows are untouched</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md Step 5 Gate WB offer condition to include [-] (greenfield)</name>
  <files>skills/project/SKILL.md</files>
  <read_first>
    - skills/project/SKILL.md — read the full file to locate Step 5 (## Step 5 -- Route) and confirm the exact current text of the Gate WB offer condition block starting at "**Gate WB offer (DD-11, D-08):**"
  </read_first>
  <action>
    In `skills/project/SKILL.md`, update the Gate WB offer condition in Step 5 (## Step 5 -- Route).

    Find this exact text (lines ~129–131):
    ```markdown
    **Gate WB offer (DD-11, D-08):** If Gate 0 is approved, no `docs/working-backwards.md`
    exists, Gate WB has not been offered yet, and the customer outcome is unclear -- offer
    Gate WB using `AskUserQuestion` with options:
    ```

    Replace with this exact text (per D-02 and CONTEXT.md specifics):
    ```markdown
    **Gate WB offer (DD-11, D-08):** If Gate 0 is approved (`[x]`) or skipped (`[-]` greenfield),
    no `docs/working-backwards.md` exists, Gate WB has not been offered yet, and the customer
    outcome is unclear -- offer Gate WB using `AskUserQuestion` with options:
    ```

    This is the only change in SKILL.md. Do NOT:
    - Modify Step 2 (Bootstrap) — it already handles greenfield correctly
    - Modify Step 3 or Step 4 — no changes needed
    - Change any other part of Step 5
    - Modify any other section of the file
  </action>
  <verify>
    <automated>cd /Users/christian/git-repos/OCC-github/agentic-ai && grep -q "\[-\]" skills/project/SKILL.md && grep -q "greenfield" skills/project/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\[-\]" skills/project/SKILL.md` exits 0
    - `grep -q "greenfield" skills/project/SKILL.md` exits 0 (the word "greenfield" now appears in Step 5 Gate WB condition, in addition to existing Step 2 usage)
    - `grep -c "greenfield" skills/project/SKILL.md` returns 2 or more (Step 2 bootstrap already uses "greenfield"; Step 5 now adds another occurrence)
    - `grep -q "approved (\[x\])" skills/project/SKILL.md` exits 0 — confirms both `[x]` and `[-]` are explicitly named in the condition
    - The file still has `disable-model-invocation: true` in its frontmatter — `grep -q "disable-model-invocation: true" skills/project/SKILL.md` exits 0
    - Line count change is minimal — the update replaces one 3-line block with another 3-line block (net zero line delta)
  </acceptance_criteria>
  <done>SKILL.md Step 5 Gate WB offer condition fires for both Gate 0 `[x]` (approved) and Gate 0 `[-]` (greenfield skip); greenfield users will be offered Gate WB on the second /project invocation</done>
</task>

</tasks>

<verification>
Run the full validation suite from 08-VALIDATION.md after both tasks complete:

```bash
cd /Users/christian/git-repos/OCC-github/agentic-ai && bash -c 'grep -q "equivalent to" skills/project/references/routing-logic.md && grep -q "skipped.*greenfield\|greenfield.*skipped\|\[-\].*greenfield" skills/project/SKILL.md && echo ALL_PASS'
```

Expected output: `ALL_PASS`

Per-requirement checks:
- PROJ-03: `grep -q "Gate 0 routing\|Gate 0 resolved" skills/project/references/routing-logic.md && echo PROJ-03_PASS`
- PROJ-06: `grep -q "\[-\]" skills/project/SKILL.md && grep -q "greenfield" skills/project/SKILL.md && echo PROJ-06_PASS`
</verification>

<success_criteria>
1. `routing-logic.md` Notes section contains an equivalence note stating Gate 0 `[-]` = `[x]` for all downstream routing
2. `routing-logic.md` Notes note scopes itself to "Gate 0 routing" or "Gate 0 resolved" — not ambiguous about which `[-]` it means
3. `SKILL.md` Step 5 Gate WB offer condition explicitly names both `[x]` and `[-]` (greenfield) as triggers
4. Full validation suite exits 0 with output `ALL_PASS`
5. No other files modified
6. Routing table rows in `routing-logic.md` are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/08-greenfield-routing/08-01-SUMMARY.md` using the summary template at `.claude/get-shit-done/templates/summary.md`.
</output>
