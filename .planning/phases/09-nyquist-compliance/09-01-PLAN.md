---
phase: 09-nyquist-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-project-router/01-VALIDATION.md
  - .planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md
  - .planning/phases/03-design-gate-2/03-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 1 VALIDATION.md has wave_0_complete: true and nyquist_compliant: true in frontmatter"
    - "Phase 2 VALIDATION.md has wave_0_complete: true and nyquist_compliant: true in frontmatter"
    - "Phase 3 VALIDATION.md has wave_0_complete: true and nyquist_compliant: true in frontmatter"
    - "Phases 1-3 VALIDATION.md files each have content-type grep checks that execute with exit code 0"
    - "All existing manual rows are preserved in each Per-Task Verification Map"
  artifacts:
    - path: ".planning/phases/01-project-router/01-VALIDATION.md"
      provides: "Phase 1 nyquist-compliant validation strategy"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md"
      provides: "Phase 2 nyquist-compliant validation strategy"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/03-design-gate-2/03-VALIDATION.md"
      provides: "Phase 3 nyquist-compliant validation strategy"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "01-VALIDATION.md Per-Task Verification Map"
      to: "skills/project/SKILL.md"
      via: "grep -q commands"
      pattern: "grep -q.*skills/project/SKILL.md"
    - from: "02-VALIDATION.md Per-Task Verification Map"
      to: "skills/project/define/SKILL.md"
      via: "grep -q commands"
      pattern: "grep -q.*skills/project/define/SKILL.md"
    - from: "03-VALIDATION.md Per-Task Verification Map"
      to: "skills/project/design/SKILL.md"
      via: "grep -q commands"
      pattern: "grep -q.*skills/project/design/SKILL.md"
---

<objective>
Upgrade phases 1, 2, and 3 VALIDATION.md files from manual-only to nyquist-compliant by adding verified grep/content bash checks (additive — manual rows stay), updating frontmatter flags, and signing off.

Purpose: Close the systematic Nyquist compliance gap for the first three pipeline phases.
Output: Three VALIDATION.md files with `nyquist_compliant: true`, `wave_0_complete: true`, verified content checks that exit 0, and completed sign-off blocks.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-nyquist-compliance/09-CONTEXT.md
@.planning/phases/09-nyquist-compliance/09-RESEARCH.md

<interfaces>
<!-- Gold standard pattern: Phase 8 VALIDATION.md structure -->
<!-- Per-Task Verification Map row format for content check:                              -->
<!-- | 01-01-01 | 01 | 1 | PROJ-01 | content | `grep -q 'term' path && echo PASS` | ✅ | ⬜ pending | -->
<!--                                                                                       -->
<!-- Frontmatter upgrade:                                                                  -->
<!-- nyquist_compliant: false  →  nyquist_compliant: true                                 -->
<!-- wave_0_complete: false    →  wave_0_complete: true                                   -->
<!--                                                                                       -->
<!-- Lint path: cicd/lint-markdown.sh  (scripts/ does NOT exist — do NOT change this)    -->
<!--                                                                                       -->
<!-- CRITICAL CONSTRAINT: D-01 says content checks are ADDITIVE.                          -->
<!-- All existing manual rows MUST remain unchanged in the verification map.              -->
<!-- Add new content-type rows for requirements that support grep-based checking.         -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Phase 1 (project-router) VALIDATION.md</name>
  <files>.planning/phases/01-project-router/01-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-project-router/01-VALIDATION.md (current file — read before touching)
    - .planning/phases/08-greenfield-routing/08-VALIDATION.md (gold standard pattern)
    - skills/project/SKILL.md (file the grep commands target — verify terms exist before writing)
    - skills/project/references/routing-logic.md (file the grep commands target)
  </read_first>
  <action>
**Step 1: Run each grep command against the actual files to confirm exit 0 BEFORE writing anything.**

Execute these commands (all must return exit 0):
```bash
grep -q 'bootstrap\|Bootstrap' skills/project/SKILL.md && echo PASS
grep -q 'routing-logic' skills/project/SKILL.md && echo PASS
grep -q 'Read-only after bootstrap' skills/project/SKILL.md && echo PASS
grep -q 'working-backwards' skills/project/SKILL.md && echo PASS
grep -q 'consistency' skills/project/SKILL.md && echo PASS
grep -q 'progress.txt' skills/project/SKILL.md && echo PASS
grep -q 'equivalent to' skills/project/references/routing-logic.md && echo PASS
bash cicd/lint-markdown.sh && echo LINT_PASS
```

If any command returns non-zero, STOP and report the failure. Do not write to the file.

**Step 2: Update the VALIDATION.md with these exact changes:**

2a. Change frontmatter:
```yaml
nyquist_compliant: true
wave_0_complete: true
```

2b. Replace the Test Infrastructure table's Quick run command and Full suite command:
```
| **Quick run command** | `bash -c 'grep -q "bootstrap" skills/project/SKILL.md && echo PASS'` |
| **Full suite command** | `bash -c 'grep -q "bootstrap\|Bootstrap" skills/project/SKILL.md && grep -q "routing-logic" skills/project/SKILL.md && grep -q "Read-only after bootstrap" skills/project/SKILL.md && grep -q "progress.txt" skills/project/SKILL.md && grep -q "equivalent to" skills/project/references/routing-logic.md && echo ALL_PASS'` |
```

2c. Add content-type rows to the Per-Task Verification Map AFTER all existing manual rows (do NOT remove any existing rows). Add these rows before the closing `*Status:` line:
```
| 01-01-C1 | 01 | 1 | PROJ-01 | content | `grep -q 'bootstrap\|Bootstrap' skills/project/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 01-01-C2 | 01 | 1 | PROJ-02/03 | content | `grep -q 'routing-logic' skills/project/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 01-01-C3 | 01 | 1 | PROJ-10 | content | `grep -q 'Read-only after bootstrap' skills/project/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 01-01-C4 | 01 | 1 | PROJ-06 | content | `grep -q 'working-backwards' skills/project/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 01-01-C5 | 01 | 1 | PROJ-05 | content | `grep -q 'consistency' skills/project/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 01-01-C6 | 01 | 1 | STATE-01/02 | content | `grep -q 'progress.txt' skills/project/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 01-01-C7 | 01 | 1 | PROJ-03 | content | `grep -q 'equivalent to' skills/project/references/routing-logic.md && echo PASS` | ✅ | ⬜ pending |
```

2d. Update Wave 0 Requirements section text to:
```
Existing infrastructure covers all phase requirements. Both target files (`skills/project/SKILL.md` and `skills/project/references/routing-logic.md`) exist on disk. No new files, no framework setup required.
```

2e. Update Validation Sign-Off to check all boxes and set Approval date:
```markdown
## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** 2026-04-03
```

**Lint path note:** The existing `bash cicd/lint-markdown.sh` references in the Sampling Rate section are CORRECT — do NOT change them to `scripts/lint-markdown.sh`. The `scripts/` path does not exist on disk. D-03 as written would break the lint check; the working path is `cicd/lint-markdown.sh`.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/01-project-router/01-VALIDATION.md && grep -q 'wave_0_complete: true' .planning/phases/01-project-router/01-VALIDATION.md && grep -q 'equivalent to' .planning/phases/01-project-router/01-VALIDATION.md && grep -q 'ALL_PASS' .planning/phases/01-project-router/01-VALIDATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-project-router/01-VALIDATION.md` contains `nyquist_compliant: true` in frontmatter
    - `.planning/phases/01-project-router/01-VALIDATION.md` contains `wave_0_complete: true` in frontmatter
    - File contains at least 7 rows starting with `| 01-01-C` (the new content-type rows)
    - All 14 original manual rows (`01-01-01` through `01-01-14`) remain present
    - File contains `ALL_PASS` string (full suite command)
    - Sign-Off block has all boxes checked: `- [x]` appears 6 times
    - Sign-Off `**Approval:**` shows `2026-04-03` not `pending`
    - All grep commands in the file, when executed, return exit code 0
  </acceptance_criteria>
  <done>Phase 1 VALIDATION.md is nyquist_compliant with verified content checks; all original manual rows preserved; sign-off complete.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Phase 2 (define) VALIDATION.md</name>
  <files>.planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md (current file — read before touching)
    - skills/project/define/SKILL.md (file the grep commands target — verify terms exist before writing)
  </read_first>
  <action>
**Step 1: Run each grep command against the actual file to confirm exit 0 BEFORE writing anything.**

Execute these commands (all must return exit 0):
```bash
grep -q 'greenfield\|Greenfield' skills/project/define/SKILL.md && echo PASS
grep -q 'Gate 0' skills/project/define/SKILL.md && echo PASS
grep -q 'codebase-assessment' skills/project/define/SKILL.md && echo PASS
grep -q 'working-backwards\|Working Backwards' skills/project/define/SKILL.md && echo PASS
grep -q 'prd.md' skills/project/define/SKILL.md && echo PASS
grep -q 'progress.txt' skills/project/define/SKILL.md && echo PASS
```

If any command returns non-zero, STOP and report the failure. Do not write to the file.

**Step 2: Update the VALIDATION.md with these exact changes:**

2a. Change frontmatter:
```yaml
nyquist_compliant: true
wave_0_complete: true
```

2b. Replace Test Infrastructure table Quick run and Full suite commands:
```
| **Quick run command** | `bash -c 'grep -q "Gate 0" skills/project/define/SKILL.md && echo PASS'` |
| **Full suite command** | `bash -c 'grep -q "greenfield\|Greenfield" skills/project/define/SKILL.md && grep -q "Gate 0" skills/project/define/SKILL.md && grep -q "codebase-assessment" skills/project/define/SKILL.md && grep -q "prd.md" skills/project/define/SKILL.md && grep -q "progress.txt" skills/project/define/SKILL.md && echo ALL_PASS'` |
```

2c. Add content-type rows to the Per-Task Verification Map AFTER all 16 existing manual rows. Add these rows before the closing `*Status:` line:
```
| 02-01-C1 | 01 | 1 | DEF-01 | content | `grep -q 'greenfield\|Greenfield' skills/project/define/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 02-01-C2 | 01 | 1 | DEF-02/03 | content | `grep -q 'Gate 0' skills/project/define/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 02-01-C3 | 01 | 1 | DEF-03 | content | `grep -q 'codebase-assessment' skills/project/define/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 02-01-C4 | 01 | 1 | DEF-09 | content | `grep -q 'working-backwards\|Working Backwards' skills/project/define/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 02-01-C5 | 01 | 1 | DEF-10/11 | content | `grep -q 'prd.md' skills/project/define/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 02-01-C6 | 01 | 1 | DEF-07/13 | content | `grep -q 'progress.txt' skills/project/define/SKILL.md && echo PASS` | ✅ | ⬜ pending |
```

2d. Update Wave 0 Requirements section text to:
```
Existing infrastructure covers all phase requirements. Target file (`skills/project/define/SKILL.md`) exists on disk. No new files, no framework setup required.
```

2e. Update Validation Sign-Off to check all boxes and set Approval date:
```markdown
## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** 2026-04-03
```

**Lint path note:** The existing `bash cicd/lint-markdown.sh` references are CORRECT — do NOT change them to `scripts/lint-markdown.sh`.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md && grep -q 'wave_0_complete: true' .planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md && grep -q 'ALL_PASS' .planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md` contains `nyquist_compliant: true` in frontmatter
    - `.planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md` contains `wave_0_complete: true` in frontmatter
    - File contains at least 6 rows starting with `| 02-01-C` (the new content-type rows)
    - All 16 original manual rows (`02-01-01` through `02-01-16`) remain present
    - File contains `ALL_PASS` string (full suite command)
    - Sign-Off block has all boxes checked: `- [x]` appears 6 times
    - Sign-Off `**Approval:**` shows `2026-04-03` not `pending`
    - All grep commands in the file, when executed, return exit code 0
  </acceptance_criteria>
  <done>Phase 2 VALIDATION.md is nyquist_compliant with verified content checks; all original manual rows preserved; sign-off complete.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade Phase 3 (design) VALIDATION.md</name>
  <files>.planning/phases/03-design-gate-2/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-design-gate-2/03-VALIDATION.md (current file — read before touching)
    - skills/project/design/SKILL.md (file the grep commands target — verify terms exist before writing)
  </read_first>
  <action>
**Step 1: Run each grep command against the actual file to confirm exit 0 BEFORE writing anything.**

Execute these commands (all must return exit 0):
```bash
grep -q 'Gate 1' skills/project/design/SKILL.md && echo PASS
grep -q 'Gate 2' skills/project/design/SKILL.md && echo PASS
grep -q 'ARCHITECTURE_AND_DESIGN' skills/project/design/SKILL.md && echo PASS
grep -q 'refresh\|Refresh' skills/project/design/SKILL.md && echo PASS
grep -q 'deviation' skills/project/design/SKILL.md && echo PASS
```

If any command returns non-zero, STOP and report the failure. Do not write to the file.

**Step 2: Update the VALIDATION.md with these exact changes:**

2a. Change frontmatter:
```yaml
nyquist_compliant: true
wave_0_complete: true
```

2b. Replace Test Infrastructure table Quick run and Full suite commands:
```
| **Quick run command** | `bash -c 'grep -q "Gate 2" skills/project/design/SKILL.md && echo PASS'` |
| **Full suite command** | `bash -c 'grep -q "Gate 1" skills/project/design/SKILL.md && grep -q "Gate 2" skills/project/design/SKILL.md && grep -q "ARCHITECTURE_AND_DESIGN" skills/project/design/SKILL.md && grep -q "refresh\|Refresh" skills/project/design/SKILL.md && grep -q "deviation" skills/project/design/SKILL.md && echo ALL_PASS'` |
```

2c. Add content-type rows to the Per-Task Verification Map AFTER all 8 existing manual rows. Add these rows before the closing `*Status:` line:
```
| 03-01-C1 | 01 | 1 | DES-01 | content | `grep -q 'Gate 1' skills/project/design/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 03-01-C2 | 01 | 1 | DES-03/07 | content | `grep -q 'Gate 2' skills/project/design/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 03-01-C3 | 01 | 1 | DES-03 | content | `grep -q 'ARCHITECTURE_AND_DESIGN' skills/project/design/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 03-01-C4 | 01 | 1 | DES-08 | content | `grep -q 'refresh\|Refresh' skills/project/design/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 03-01-C5 | 01 | 1 | DES-08 | content | `grep -q 'deviation' skills/project/design/SKILL.md && echo PASS` | ✅ | ⬜ pending |
```

2d. Update Wave 0 Requirements section text to:
```
Existing infrastructure covers all phase requirements. Target file (`skills/project/design/SKILL.md`) exists on disk. No new files, no framework setup required.
```

2e. Update Validation Sign-Off to check all boxes and set Approval date:
```markdown
## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** 2026-04-03
```

**Lint path note:** The existing `bash cicd/lint-markdown.sh` references are CORRECT — do NOT change them to `scripts/lint-markdown.sh`.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/03-design-gate-2/03-VALIDATION.md && grep -q 'wave_0_complete: true' .planning/phases/03-design-gate-2/03-VALIDATION.md && grep -q 'ALL_PASS' .planning/phases/03-design-gate-2/03-VALIDATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-design-gate-2/03-VALIDATION.md` contains `nyquist_compliant: true` in frontmatter
    - `.planning/phases/03-design-gate-2/03-VALIDATION.md` contains `wave_0_complete: true` in frontmatter
    - File contains at least 5 rows starting with `| 03-01-C` (the new content-type rows)
    - All 8 original manual rows (`03-01-01` through `03-01-08`) remain present
    - File contains `ALL_PASS` string (full suite command)
    - Sign-Off block has all boxes checked: `- [x]` appears 6 times
    - Sign-Off `**Approval:**` shows `2026-04-03` not `pending`
    - All grep commands in the file, when executed, return exit code 0
  </acceptance_criteria>
  <done>Phase 3 VALIDATION.md is nyquist_compliant with verified content checks; all original manual rows preserved; sign-off complete.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run:

```bash
bash -c '
grep -q "nyquist_compliant: true" .planning/phases/01-project-router/01-VALIDATION.md && echo "Phase 1: OK" || echo "Phase 1: FAIL"
grep -q "nyquist_compliant: true" .planning/phases/02-define-gates-0-wb-1/02-VALIDATION.md && echo "Phase 2: OK" || echo "Phase 2: FAIL"
grep -q "nyquist_compliant: true" .planning/phases/03-design-gate-2/03-VALIDATION.md && echo "Phase 3: OK" || echo "Phase 3: FAIL"
'
```

All three must print OK.
</verification>

<success_criteria>
- Three VALIDATION.md files updated: phases 1, 2, 3
- Each has `nyquist_compliant: true` and `wave_0_complete: true` in frontmatter
- Each has new content-type grep rows added (not replacing) the existing manual rows
- Each full suite command exits 0 when run
- Each sign-off block has all boxes checked and Approval date set
- `cicd/lint-markdown.sh` path preserved as-is (not changed to `scripts/`)
</success_criteria>

<output>
After completion, create `.planning/phases/09-nyquist-compliance/09-01-SUMMARY.md`
</output>
