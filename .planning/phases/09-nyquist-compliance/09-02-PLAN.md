---
phase: 09-nyquist-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-milestone-gate-3/04-VALIDATION.md
  - .planning/phases/05-plan-gate-4/05-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 4 VALIDATION.md has wave_0_complete: true and nyquist_compliant: true in frontmatter"
    - "Phase 5 VALIDATION.md has wave_0_complete: true and nyquist_compliant: true in frontmatter"
    - "Phases 4-5 VALIDATION.md files each have content-type grep checks that execute with exit code 0"
    - "All existing manual rows are preserved in each Per-Task Verification Map"
  artifacts:
    - path: ".planning/phases/04-milestone-gate-3/04-VALIDATION.md"
      provides: "Phase 4 nyquist-compliant validation strategy"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/05-plan-gate-4/05-VALIDATION.md"
      provides: "Phase 5 nyquist-compliant validation strategy"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "04-VALIDATION.md Per-Task Verification Map"
      to: "skills/project/milestone/SKILL.md"
      via: "grep -q commands"
      pattern: "grep -q.*skills/project/milestone/SKILL.md"
    - from: "05-VALIDATION.md Per-Task Verification Map"
      to: "skills/project/plan/SKILL.md"
      via: "grep -q commands"
      pattern: "grep -q.*skills/project/plan/SKILL.md"
---

<objective>
Upgrade phases 4 and 5 VALIDATION.md files from manual-only to nyquist-compliant by adding verified grep/content bash checks (additive — manual rows stay), updating frontmatter flags, and signing off.

Purpose: Close the systematic Nyquist compliance gap for the milestone and plan pipeline phases.
Output: Two VALIDATION.md files with `nyquist_compliant: true`, `wave_0_complete: true`, verified content checks that exit 0, and completed sign-off blocks.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-nyquist-compliance/09-CONTEXT.md
@.planning/phases/09-nyquist-compliance/09-RESEARCH.md

<interfaces>
<!-- Gold standard pattern: Phase 8 VALIDATION.md structure -->
<!-- Per-Task Verification Map row format for content check:                              -->
<!-- | 04-01-C1 | 01 | 1 | MIL-01 | content | `grep -q 'term' path && echo PASS` | ✅ | ⬜ pending | -->
<!--                                                                                       -->
<!-- Frontmatter upgrade:                                                                  -->
<!-- nyquist_compliant: false  →  nyquist_compliant: true                                 -->
<!-- wave_0_complete: false    →  wave_0_complete: true                                   -->
<!--                                                                                       -->
<!-- Lint path: cicd/lint-markdown.sh is CORRECT — do NOT change to scripts/             -->
<!-- scripts/lint-markdown.sh does NOT exist on disk                                      -->
<!--                                                                                       -->
<!-- CRITICAL CONSTRAINT: D-01 says content checks are ADDITIVE.                          -->
<!-- All existing manual-only rows MUST remain unchanged in the verification map.         -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Phase 4 (milestone) VALIDATION.md</name>
  <files>.planning/phases/04-milestone-gate-3/04-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04-milestone-gate-3/04-VALIDATION.md (current file — read before touching)
    - skills/project/milestone/SKILL.md (file the grep commands target — verify terms exist before writing)
  </read_first>
  <action>
**Step 1: Run each grep command against the actual file to confirm exit 0 BEFORE writing anything.**

Execute these commands (all must return exit 0):
```bash
grep -q 'Gate 2' skills/project/milestone/SKILL.md && echo PASS
grep -q 'Gate 3' skills/project/milestone/SKILL.md && echo PASS
grep -q 'milestone-status' skills/project/milestone/SKILL.md && echo PASS
grep -q 'In progress' skills/project/milestone/SKILL.md && echo PASS
grep -q 'revision\|Revision' skills/project/milestone/SKILL.md && echo PASS
```

If any command returns non-zero, STOP and report the failure. Do not write to the file.

**Step 2: Update the VALIDATION.md with these exact changes:**

2a. Change frontmatter:
```yaml
nyquist_compliant: true
wave_0_complete: true
```

2b. Replace Test Infrastructure table Quick run and Full suite commands:
```
| **Quick run command** | `bash -c 'grep -q "Gate 3" skills/project/milestone/SKILL.md && echo PASS'` |
| **Full suite command** | `bash -c 'grep -q "Gate 2" skills/project/milestone/SKILL.md && grep -q "Gate 3" skills/project/milestone/SKILL.md && grep -q "milestone-status" skills/project/milestone/SKILL.md && grep -q "In progress" skills/project/milestone/SKILL.md && grep -q "revision\|Revision" skills/project/milestone/SKILL.md && echo ALL_PASS'` |
```

2c. Add content-type rows to the Per-Task Verification Map AFTER all 13 existing manual-only rows. Add these rows before the closing `*Status:` line:
```
| 04-01-C1 | 01 | 1 | MIL-01 | content | `grep -q 'Gate 2' skills/project/milestone/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 04-01-C2 | 01 | 1 | MIL-09 | content | `grep -q 'Gate 3' skills/project/milestone/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 04-01-C3 | 01 | 1 | MIL-05/08/11 | content | `grep -q 'milestone-status' skills/project/milestone/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 04-01-C4 | 01 | 1 | MIL-09 | content | `grep -q 'In progress' skills/project/milestone/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 04-01-C5 | 01 | 1 | MIL-10/11/12 | content | `grep -q 'revision\|Revision' skills/project/milestone/SKILL.md && echo PASS` | ✅ | ⬜ pending |
```

2d. Update Wave 0 Requirements section text to:
```
Existing infrastructure covers all phase requirements. Target file (`skills/project/milestone/SKILL.md`) exists on disk. No new files, no framework setup required.
```

2e. Update Validation Sign-Off to check all boxes and set Approval date:
```markdown
## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** 2026-04-03
```

**Lint path note:** The existing `bash cicd/lint-markdown.sh` references are CORRECT — do NOT change them to `scripts/lint-markdown.sh`.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/04-milestone-gate-3/04-VALIDATION.md && grep -q 'wave_0_complete: true' .planning/phases/04-milestone-gate-3/04-VALIDATION.md && grep -q 'ALL_PASS' .planning/phases/04-milestone-gate-3/04-VALIDATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-milestone-gate-3/04-VALIDATION.md` contains `nyquist_compliant: true` in frontmatter
    - `.planning/phases/04-milestone-gate-3/04-VALIDATION.md` contains `wave_0_complete: true` in frontmatter
    - File contains at least 5 rows starting with `| 04-01-C` (the new content-type rows)
    - All 13 original manual-only rows (`04-01-01` through `04-01-13`) remain present
    - File contains `ALL_PASS` string (full suite command)
    - Sign-Off block has all boxes checked: `- [x]` appears 6 times
    - Sign-Off `**Approval:**` shows `2026-04-03` not `pending`
    - All grep commands in the file, when executed, return exit code 0
  </acceptance_criteria>
  <done>Phase 4 VALIDATION.md is nyquist_compliant with verified content checks; all original manual rows preserved; sign-off complete.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Phase 5 (plan) VALIDATION.md</name>
  <files>.planning/phases/05-plan-gate-4/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-plan-gate-4/05-VALIDATION.md (current file — read before touching)
    - skills/project/plan/SKILL.md (file the grep commands target — verify terms exist before writing)
  </read_first>
  <action>
**Step 1: Run each grep command against the actual file to confirm exit 0 BEFORE writing anything.**

Execute these commands (all must return exit 0):
```bash
grep -q 'Gate 4' skills/project/plan/SKILL.md && echo PASS
grep -q 'milestone-status' skills/project/plan/SKILL.md && echo PASS
grep -q 'sub-feature' skills/project/plan/SKILL.md && echo PASS
grep -q 'planned.*awaiting\|awaiting build' skills/project/plan/SKILL.md && echo PASS
grep -q 'gate-4-plan\|gate-4' skills/project/plan/SKILL.md && echo PASS
```

If any command returns non-zero, STOP and report the failure. Do not write to the file. For the `planned.*awaiting` pattern: if that exact pattern fails, try `grep -q 'awaiting' skills/project/plan/SKILL.md` as a fallback and record which pattern actually passed.

**Step 2: Update the VALIDATION.md with these exact changes:**

2a. Change frontmatter:
```yaml
nyquist_compliant: true
wave_0_complete: true
```

2b. Replace Test Infrastructure table Quick run and Full suite commands:
```
| **Quick run command** | `bash -c 'grep -q "Gate 4" skills/project/plan/SKILL.md && echo PASS'` |
| **Full suite command** | `bash -c 'grep -q "Gate 4" skills/project/plan/SKILL.md && grep -q "milestone-status" skills/project/plan/SKILL.md && grep -q "sub-feature" skills/project/plan/SKILL.md && echo ALL_PASS'` |
```

2c. Add content-type rows to the Per-Task Verification Map AFTER all 9 existing manual-only rows. Use the grep patterns that were confirmed passing in Step 1. Add these rows before the closing `*Status:` line:
```
| 05-01-C1 | 01 | 1 | PLAN-02/09 | content | `grep -q 'Gate 4' skills/project/plan/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 05-01-C2 | 01 | 1 | PLAN-06/09 | content | `grep -q 'milestone-status' skills/project/plan/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 05-01-C3 | 01 | 1 | PLAN-04 | content | `grep -q 'sub-feature' skills/project/plan/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 05-01-C4 | 01 | 1 | PLAN-09 | content | `grep -q 'awaiting build\|awaiting' skills/project/plan/SKILL.md && echo PASS` | ✅ | ⬜ pending |
| 05-01-C5 | 01 | 1 | PLAN-03 | content | `grep -q 'gate-4-plan\|gate-4' skills/project/plan/SKILL.md && echo PASS` | ✅ | ⬜ pending |
```

Note: For row C4, use whichever grep pattern passed in Step 1 (`planned.*awaiting` or just `awaiting build` or `awaiting`). Write the confirmed-passing pattern into the VALIDATION.md.

2d. Update Wave 0 Requirements section text to:
```
Existing infrastructure covers all phase requirements. Target file (`skills/project/plan/SKILL.md`) exists on disk. No new files, no framework setup required.
```

2e. Update Validation Sign-Off to check all boxes and set Approval date:
```markdown
## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** 2026-04-03
```

**Lint path note:** The existing `bash cicd/lint-markdown.sh` references are CORRECT — do NOT change them to `scripts/lint-markdown.sh`.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/05-plan-gate-4/05-VALIDATION.md && grep -q 'wave_0_complete: true' .planning/phases/05-plan-gate-4/05-VALIDATION.md && grep -q 'ALL_PASS' .planning/phases/05-plan-gate-4/05-VALIDATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-plan-gate-4/05-VALIDATION.md` contains `nyquist_compliant: true` in frontmatter
    - `.planning/phases/05-plan-gate-4/05-VALIDATION.md` contains `wave_0_complete: true` in frontmatter
    - File contains at least 5 rows starting with `| 05-01-C` (the new content-type rows)
    - All 9 original manual-only rows (`05-01-01` through `05-01-09`) remain present
    - File contains `ALL_PASS` string (full suite command)
    - Sign-Off block has all boxes checked: `- [x]` appears 6 times
    - Sign-Off `**Approval:**` shows `2026-04-03` not `pending`
    - All grep commands in the file, when executed, return exit code 0
  </acceptance_criteria>
  <done>Phase 5 VALIDATION.md is nyquist_compliant with verified content checks; all original manual rows preserved; sign-off complete.</done>
</task>

</tasks>

<verification>
After both tasks complete, run:

```bash
bash -c '
grep -q "nyquist_compliant: true" .planning/phases/04-milestone-gate-3/04-VALIDATION.md && echo "Phase 4: OK" || echo "Phase 4: FAIL"
grep -q "nyquist_compliant: true" .planning/phases/05-plan-gate-4/05-VALIDATION.md && echo "Phase 5: OK" || echo "Phase 5: FAIL"
'
```

Both must print OK.
</verification>

<success_criteria>
- Two VALIDATION.md files updated: phases 4 and 5
- Each has `nyquist_compliant: true` and `wave_0_complete: true` in frontmatter
- Each has new content-type grep rows added (not replacing) the existing manual rows
- Each full suite command exits 0 when run
- Each sign-off block has all boxes checked and Approval date set
- `cicd/lint-markdown.sh` path preserved as-is
</success_criteria>

<output>
After completion, create `.planning/phases/09-nyquist-compliance/09-02-SUMMARY.md`
</output>
