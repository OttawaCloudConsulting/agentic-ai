---
phase: 10-tech-debt-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - docs/SKILLS.md
  - skills/project/SKILL.md
  - .planning/phases/04-milestone-gate-3/04-03-SUMMARY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "ROADMAP.md plan-level checkboxes are accurate — completed plans show [x]"
    - "docs/SKILLS.md cp block has entries for all 6 project sub-skills (define, design, milestone, plan, build, spike)"
    - "skills/project/SKILL.md STATE-04 attribution does not list /plan as subject to write-ordering"
    - "04-03-SUMMARY.md frontmatter contains a requirements-completed field listing all 13 MIL requirements"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Accurate plan-level checkboxes for phases 1-9"
      contains: "[x] 01-03-PLAN.md"
    - path: "docs/SKILLS.md"
      provides: "Complete cp block covering all 6 project sub-skills"
      contains: "cp -r skills/project/define/"
    - path: "skills/project/SKILL.md"
      provides: "Correct STATE-04 attribution (build + milestone only)"
      contains: "This rule applies to `/build` and `/milestone`, not to `/project` or `/plan`."
    - path: ".planning/phases/04-milestone-gate-3/04-03-SUMMARY.md"
      provides: "Complete frontmatter with requirements-completed field"
      contains: "requirements-completed: [MIL-01"
  key_links:
    - from: "docs/SKILLS.md"
      to: "skills/project/define/"
      via: "cp -r command in Consuming Skills section"
      pattern: "cp -r skills/project/define/"
    - from: "skills/project/SKILL.md"
      to: "STATE-04 rule"
      via: "write-ordering attribution note at line 27"
      pattern: "This rule applies to"
---

<objective>
Fix 4 low-severity documentation and attribution issues identified in the v1.0 audit.

Purpose: Close the SKILLS.md copy commands inconsistency, STATE-04 attribution error, 04-03-SUMMARY.md missing frontmatter, and ROADMAP.md stale checkboxes. All changes are in-place text edits — no behavioral or logic changes to any skill.
Output: 4 files with targeted text edits, all verifiable by grep.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-tech-debt-sweep/10-CONTEXT.md
@.planning/phases/10-tech-debt-sweep/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md stale plan checkboxes (per D-01, D-02)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — read the full file before editing; confirm line 44 shows `- [ ] 01-03-PLAN.md` and line 110 shows `- [ ] 05-02-PLAN.md`; also scan all other plan-level entries in phases 1-9 to confirm D-03 (no other changes needed)
  </read_first>
  <action>
Make exactly 2 targeted edits to `.planning/ROADMAP.md`:

Edit 1 (per D-01) — Phase 1 plans block, line 44:
  Before: - [ ] 01-03-PLAN.md — Documentation (detail doc + catalog entry)
  After:  - [x] 01-03-PLAN.md — Documentation (detail doc + catalog entry)

Edit 2 (per D-02) — Phase 5 plans block, line 110:
  Before: - [ ] 05-02-PLAN.md — SKILL.md flow controller
  After:  - [x] 05-02-PLAN.md — SKILL.md flow controller

No other checkbox changes. D-03 is authoritative: phases 1-9 top-level entries already show `[x]`, phases 10-11 correctly show `[ ]`. Do NOT change any other lines.
  </action>
  <verify>
    <automated>grep -n "01-03-PLAN\|05-02-PLAN" /Users/christian/git-repos/OCC-github/agentic-ai/.planning/ROADMAP.md</automated>
  </verify>
  <done>
    - Line 44 shows `- [x] 01-03-PLAN.md — Documentation (detail doc + catalog entry)`
    - Line 110 shows `- [x] 05-02-PLAN.md — SKILL.md flow controller`
    - No other plan-level checkboxes were changed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add missing cp commands to docs/SKILLS.md (per D-04, D-05)</name>
  <files>docs/SKILLS.md</files>
  <read_first>
    - docs/SKILLS.md — read the full file before editing; identify the Consuming Skills bash block (lines 67-82); confirm the existing entries for milestone/, plan/, spike/ and the catch-all `cp -r skills/project/`; note the exact spacing/alignment pattern used by the existing project sub-skill entries
  </read_first>
  <action>
In `docs/SKILLS.md`, inside the Consuming Skills bash block, insert 3 new lines for `define/`, `design/`, and `build/` after the existing `spike/` entry and before the `occ-skill-creator/` line (per D-05, the catch-all `cp -r skills/project/` line remains unchanged).

Current state of the block (lines 75-81):

    cp -r skills/project/milestone/          [target-repo]/.claude/skills/project/milestone/
    cp -r skills/project/plan/                [target-repo]/.claude/skills/project/plan/
    cp -r skills/project/spike/               [target-repo]/.claude/skills/project/spike/
    cp -r skills/occ-skill-creator/         [target-repo]/.claude/skills/occ-skill-creator/
    cp -r skills/occ-skill-refactor/        [target-repo]/.claude/skills/occ-skill-refactor/
    cp -r skills/project/                   [target-repo]/.claude/skills/project/

Insert 3 new lines after the spike/ line, in pipeline invocation order (define, design, build), matching existing alignment. Exact lines to add:

    cp -r skills/project/define/              [target-repo]/.claude/skills/project/define/
    cp -r skills/project/design/              [target-repo]/.claude/skills/project/design/
    cp -r skills/project/build/               [target-repo]/.claude/skills/project/build/

NOTE: In the actual file, [target-repo] is written as angle-bracket target-repo angle-bracket (less-than sign, "target-repo", greater-than sign). Copy the destination format exactly from the existing spike/, plan/, and milestone/ lines — do not invent a new format.

Alignment rule: match the visual column alignment of the existing milestone/, plan/, spike/ entries (the destination paths start at approximately column 47).

Do NOT add, remove, or change any other lines in the file.
  </action>
  <verify>
    <automated>grep "cp -r skills/project/define\|cp -r skills/project/design\|cp -r skills/project/build" /Users/christian/git-repos/OCC-github/agentic-ai/docs/SKILLS.md</automated>
  </verify>
  <done>
    - grep returns exactly 3 matches (one each for define/, design/, build/)
    - The catch-all `cp -r skills/project/` line still exists unchanged
    - No other lines in the file were changed
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix SKILL.md STATE-04 attribution and add 04-03-SUMMARY.md frontmatter field (per D-06, D-07, D-08)</name>
  <files>skills/project/SKILL.md, .planning/phases/04-milestone-gate-3/04-03-SUMMARY.md</files>
  <read_first>
    - skills/project/SKILL.md — read lines 24-30 to see the current attribution text at line 27: "This rule applies to `/build`, `/milestone`, and `/plan`, not to `/project` itself."
    - .planning/phases/04-milestone-gate-3/04-03-SUMMARY.md — read the full frontmatter block (lines 1-15, between the two YAML delimiters) to confirm `tags:` is the last frontmatter field and `requirements-completed:` is absent
    - .planning/phases/04-milestone-gate-3/04-01-SUMMARY.md — verify the requirements-completed field pattern at line 43 (will match exactly: `requirements-completed: [MIL-01, MIL-02, MIL-03, MIL-04, MIL-05, MIL-06, MIL-07, MIL-08, MIL-09, MIL-10, MIL-11, MIL-12, MIL-13]`)
  </read_first>
  <action>
EDIT A — skills/project/SKILL.md line 27 (per D-06, D-07):

Find the write-ordering rule note in the Rules section. Current text spans lines 26-28:
  ordering (STATE-04). This rule applies to `/build`, `/milestone`, and `/plan`, not to
  `/project` itself.

Replace those two lines with one line:
  ordering (STATE-04). This rule applies to `/build` and `/milestone`, not to `/project` or `/plan`.

The surrounding bullet (`- **Write-ordering contract for downstream skills.**`) that begins on line 25 must remain intact. Only the attribution sentence changes. Do NOT touch any other lines in SKILL.md.


EDIT B — .planning/phases/04-milestone-gate-3/04-03-SUMMARY.md (per D-08):

Inside the YAML frontmatter block (between the opening and closing YAML delimiters), the current last field is:
  tags: [milestone, gate-3, documentation, catalog]

Add this line immediately after the tags: line (still inside the frontmatter, before the closing delimiter):
  requirements-completed: [MIL-01, MIL-02, MIL-03, MIL-04, MIL-05, MIL-06, MIL-07, MIL-08, MIL-09, MIL-10, MIL-11, MIL-12, MIL-13]

CRITICAL PLACEMENT: the new field must be inside the frontmatter block, between the opening and closing YAML delimiter lines, NOT after the closing delimiter. The body of the SUMMARY (the section starting with `# Dependency graph`) must remain completely unchanged.
  </action>
  <verify>
    <automated>grep "This rule applies" /Users/christian/git-repos/OCC-github/agentic-ai/skills/project/SKILL.md && grep "requirements-completed" /Users/christian/git-repos/OCC-github/agentic-ai/.planning/phases/04-milestone-gate-3/04-03-SUMMARY.md</automated>
  </verify>
  <done>
    - `skills/project/SKILL.md`: grep for "This rule applies" returns one line; that line contains "build" and "milestone" but does NOT list "/plan" in the applies-to portion. Exact expected text: "This rule applies to `/build` and `/milestone`, not to `/project` or `/plan`."
    - `04-03-SUMMARY.md`: grep for "requirements-completed" returns one line containing `MIL-01` through `MIL-13`
    - The 04-03-SUMMARY.md body content (everything after the closing YAML delimiter) is unchanged
  </done>
</task>

</tasks>

<verification>
Run all 4 grep checks after all tasks are complete:

  Fix 1 — ROADMAP.md plan checkboxes (both lines must show [x]):
    grep -n "01-03-PLAN\|05-02-PLAN" .planning/ROADMAP.md

  Fix 2 — SKILLS.md has all 3 new cp lines:
    grep "cp -r skills/project/define\|cp -r skills/project/design\|cp -r skills/project/build" docs/SKILLS.md

  Fix 3 — SKILL.md attribution does not list /plan in applies-to:
    grep "This rule applies" skills/project/SKILL.md

  Fix 4 — 04-03-SUMMARY.md has requirements-completed:
    grep "requirements-completed" .planning/phases/04-milestone-gate-3/04-03-SUMMARY.md

Expected results:
  Fix 1: 2 lines, both showing [x]
  Fix 2: 3 lines returned
  Fix 3: 1 line returned, containing "build" and "milestone" without "/plan" in the applies-to portion
  Fix 4: 1 line returned, containing MIL-01 through MIL-13
</verification>

<success_criteria>
1. grep -n "01-03-PLAN\|05-02-PLAN" .planning/ROADMAP.md returns 2 lines both showing [x]
2. grep for "cp -r skills/project/define\|cp -r skills/project/design\|cp -r skills/project/build" in docs/SKILLS.md returns 3 matches
3. grep "This rule applies" skills/project/SKILL.md returns a line NOT containing "/plan" in the applies-to portion
4. grep "requirements-completed" .planning/phases/04-milestone-gate-3/04-03-SUMMARY.md returns a line with MIL-01 through MIL-13
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-debt-sweep/10-01-SUMMARY.md`
</output>
