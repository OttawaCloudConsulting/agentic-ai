---
phase: 11-gate3-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/project/SKILL.md
  - skills/project/references/routing-logic.md
  - skills/project/DESIGN.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MIL-09
  - PROJ-10

must_haves:
  truths:
    - "SKILL.md Rules section names both bootstrap and Gate 3 closure as the two exceptions to read-only"
    - "SKILL.md Step 5 contains a Gate 3 closure offer block using AskUserQuestion before normal routing output"
    - "routing-logic.md has a row for 'All milestones [x], Gate 3 still [~] In progress' inserted before the 'All milestones complete' terminal row"
    - "routing-logic.md Artifact Validation section skips sentinel paths starting with '(' to prevent spurious PROJ-04 warnings"
    - "DESIGN.md DD-3 has a 'Gate 3 closure exception' paragraph following the Bootstrap exception paragraph"
    - "REQUIREMENTS.md PROJ-10 row includes the parenthetical '(Bootstrap and Gate 3 closure are the two exceptions)'"
  artifacts:
    - path: "skills/project/SKILL.md"
      provides: "Gate 3 closure offer logic in Step 5"
      contains: "Gate 3 closure"
    - path: "skills/project/references/routing-logic.md"
      provides: "Routing table row for Gate 3 closure state + sentinel guard"
      contains: "Gate 3 still"
    - path: "skills/project/DESIGN.md"
      provides: "DD-3 Gate 3 closure exception documentation"
      contains: "Gate 3 closure exception"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROJ-10 narrowed to name both exceptions"
      contains: "Gate 3 closure are the two exceptions"
  key_links:
    - from: "skills/project/SKILL.md (Step 5)"
      to: "skills/project/references/routing-logic.md"
      via: "routing table row evaluated before terminal row"
      pattern: "Gate 3 closure"
    - from: "skills/project/SKILL.md (Rules)"
      to: ".planning/REQUIREMENTS.md (PROJ-10)"
      via: "both name same two exceptions"
      pattern: "two exceptions"
---

<objective>
Implement the Gate 3 closure pathway in `/project` and update all three documentation locations to reflect the narrowed read-only rule.

Purpose: Gate 3 currently shows `[~] In progress` permanently even on fully-completed projects because the closure pathway was never implemented. This phase closes the `GATE3-CLOSURE` integration gap from the v1.0 audit.

Output: Four files updated — SKILL.md gains a Gate 3 closure offer branch in Step 5 and an updated Rules section; routing-logic.md gains a new routing row and a sentinel guard; DESIGN.md gains a Gate 3 closure exception paragraph in DD-3; REQUIREMENTS.md PROJ-10 gains an exception parenthetical.
</objective>

<execution_context>
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christian/git-repos/OCC-github/agentic-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gate3-closure/11-CONTEXT.md
@.planning/phases/11-gate3-closure/11-RESEARCH.md
</context>

<interfaces>
<!-- Key patterns the executor needs. Extracted from live codebase. -->

From skills/project/SKILL.md (lines 20-22) — current Rules read-only text to replace:
```
- **Read-only after bootstrap.** After the initial `progress.txt` creation, `/project`
  never modifies `progress.txt` or any other file (PROJ-10). Bootstrap (Step 2) is the
  sole exception.
```

From skills/project/SKILL.md (lines 128-136) — Gate WB offer pattern to mirror for Gate 3 closure:
```markdown
**Gate WB offer (DD-11, D-08):** If Gate 0 is approved (`[x]`) or skipped (`[-]` greenfield),
no `docs/working-backwards.md` exists, Gate WB has not been offered yet, and the customer
outcome is unclear -- offer Gate WB using `AskUserQuestion` with options:

- **Yes** -- proceed with Working Backwards exercise (routes to `/define`)
- **Skip** -- record Gate WB as skipped
- **Defer** -- record Gate WB as Pending for later decision

Include a 2-3 sentence explanation of the Working Backwards value proposition.
```

From skills/project/references/routing-logic.md (lines 23-24) — insertion point for new row:
```markdown
| All features complete in active milestone, more milestones to define | Run `/milestone` for next milestone | `/design` (refresh) |
| All milestones complete | Project complete -- celebrate | `/design` (refresh) |
```

From skills/project/references/routing-logic.md (lines 95-108) — current Artifact Validation section to add sentinel guard to:
```markdown
**Process:**

1. For each gate marked `[x]` in `progress.txt`, extract the artifact path from the gate entry line (the path following the date).
2. Check if the file exists on disk at the extracted path.
3. If the file is missing, emit an inline warning (per D-06) directly after the gate entry in the status report:
```

From skills/project/DESIGN.md (lines 100-103) — Bootstrap exception paragraph (style template for Gate 3 closure paragraph):
```markdown
**Bootstrap exception:** On first run, if the project-level `progress.txt` does not exist,
`/project` creates it with gate entries and no milestones. Milestone-level `milestone-status.txt`
files are created by `/milestone` when milestones are defined. This bootstrap is the only case where
`/project` writes to disk. After bootstrap, `/project` is strictly read-only.
```

Gate 3 approved line format (from progress-format.md):
```
[x] Gate 3: Milestone Review  Approved: <YYYY-MM-DD>  (closed by /project)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Gate 3 closure offer to SKILL.md (Rules + Step 5)</name>
  <files>skills/project/SKILL.md</files>
  <read_first>
    - skills/project/SKILL.md (read the full file to see current state before editing)
    - .planning/phases/11-gate3-closure/11-CONTEXT.md (decisions D-01 through D-09)
    - .planning/phases/11-gate3-closure/11-RESEARCH.md (Pattern 1, Pattern 6, anti-patterns)
  </read_first>
  <action>
Make two targeted edits to skills/project/SKILL.md.

**Edit 1 — Rules section (lines 20-22):**
Replace the current read-only rule (3 lines):
```
- **Read-only after bootstrap.** After the initial `progress.txt` creation, `/project`
  never modifies `progress.txt` or any other file (PROJ-10). Bootstrap (Step 2) is the
  sole exception.
```
With the updated rule naming both exceptions:
```
- **Read-only after bootstrap.** After the initial `progress.txt` creation, `/project`
  never modifies `progress.txt` or any other file (PROJ-10). Two exceptions exist:
  bootstrap (Step 2) and Gate 3 closure (Step 5, when all milestones are complete).
```

**Edit 2 — Step 5 Route section, after the Gate WB offer block:**
The Gate WB offer block currently ends with the line:
```
Include a 2-3 sentence explanation of the Working Backwards value proposition.
```
Immediately after that line, append the Gate 3 closure offer block (new paragraph, blank line before):

```markdown

**Gate 3 closure offer (D-01 through D-05, PROJ-10):** If all milestones in `progress.txt`
are `[x]` complete AND Gate 3 is still `[~] In progress` -- offer Gate 3 closure using
`AskUserQuestion` with options:

- **Close Gate 3** -- write `[x] Gate 3: Milestone Review  Approved: <YYYY-MM-DD>  (closed by /project)`
  to `progress.txt` (replacing the `[~] In progress` line), where `<YYYY-MM-DD>` is today's date.
  Then continue to routing (the "All milestones complete" row will show "Project complete").
- **Leave open** -- skip the write. Continue to normal routing (the "All milestones complete"
  row will still show "Project complete").

Show a 1-line explanation before the prompt: "Gate 3 tracks milestone planning. Closing it
marks the milestone review phase officially complete."

After `AskUserQuestion` resolves (regardless of which option the user chose), continue to
normal routing table evaluation.

If the user chose **Close Gate 3**: use the Write tool to update the Gate 3 line in
`progress.txt`. Read the file first, replace `[~] Gate 3: Milestone Review             In progress`
with `[x] Gate 3: Milestone Review  Approved: <YYYY-MM-DD>  (closed by /project)`. Read the
file back and confirm the update was applied correctly before proceeding to routing.
```

Do not modify any other part of SKILL.md. Do not change Step 2 (Bootstrap), Step 3, Step 4, or Error Handling.
  </action>
  <verify>
    <automated>grep -n "two exceptions" /Users/christian/git-repos/OCC-github/agentic-ai/skills/project/SKILL.md && grep -n "Gate 3 closure offer" /Users/christian/git-repos/OCC-github/agentic-ai/skills/project/SKILL.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - skills/project/SKILL.md contains the exact string "two exceptions" in the Rules section
    - skills/project/SKILL.md contains "Gate 3 closure offer" in Step 5
    - skills/project/SKILL.md does NOT contain "sole exception" (the old text is gone)
    - skills/project/SKILL.md contains "AskUserQuestion" appearing at least twice (Gate WB offer and Gate 3 closure offer)
    - skills/project/SKILL.md contains "Close Gate 3" as an option label
    - skills/project/SKILL.md contains "Leave open" as an option label
    - grep -c "AskUserQuestion" skills/project/SKILL.md returns 2 or more
    - bash cicd/lint-markdown.sh exits 0 (no markdown lint errors introduced)
  </acceptance_criteria>
  <done>SKILL.md Rules section names both exceptions; Step 5 has a Gate 3 closure offer block following the Gate WB offer block; "sole exception" no longer appears in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Add Gate 3 routing row and sentinel guard to routing-logic.md</name>
  <files>skills/project/references/routing-logic.md</files>
  <read_first>
    - skills/project/references/routing-logic.md (read the full file before editing)
    - .planning/phases/11-gate3-closure/11-RESEARCH.md (Pattern 2, Pattern 3, Pitfall 1, Pitfall 3)
  </read_first>
  <action>
Make two targeted edits to skills/project/references/routing-logic.md.

**Edit 1 — Routing Table: insert new row before the terminal row.**
Find the current last two content rows of the routing table (lines 23-24):
```
| All features complete in active milestone, more milestones to define | Run `/milestone` for next milestone | `/design` (refresh) |
| All milestones complete | Project complete -- celebrate | `/design` (refresh) |
```
Insert a new row BETWEEN these two rows:
```
| All milestones `[x]` complete, Gate 3 still `[~] In progress` | Offer Gate 3 closure via `AskUserQuestion` (per D-01–D-05) | -- |
```
Result after edit — the last three content rows should be:
```
| All features complete in active milestone, more milestones to define | Run `/milestone` for next milestone | `/design` (refresh) |
| All milestones `[x]` complete, Gate 3 still `[~] In progress` | Offer Gate 3 closure via `AskUserQuestion` (per D-01–D-05) | -- |
| All milestones complete | Project complete -- celebrate | `/design` (refresh) |
```
Do NOT modify the "All milestones complete" terminal row.

**Edit 2 — Artifact Validation section: add sentinel path guard.**
In the Artifact Validation section, find the **Process:** ordered list. The list currently reads:
```
1. For each gate marked `[x]` in `progress.txt`, extract the artifact path from the gate entry line (the path following the date).
2. Check if the file exists on disk at the extracted path.
3. If the file is missing, emit an inline warning...
```
Add step 1a (insert between steps 1 and 2) by replacing the current step 2 with:
```
2. **Sentinel path check:** If the extracted path begins with `(` (e.g., `(closed by /project)`), skip the file-existence check — this is a sentinel value indicating no physical artifact exists. No warning is emitted for sentinel paths.
3. Check if the file exists on disk at the extracted path.
4. If the file is missing, emit an inline warning (per D-06) directly after the gate entry in the status report:
```
(Renumber the original steps 2 and 3 to 3 and 4.)
  </action>
  <verify>
    <automated>grep -n "Gate 3 still" /Users/christian/git-repos/OCC-github/agentic-ai/skills/project/references/routing-logic.md && grep -n "sentinel" /Users/christian/git-repos/OCC-github/agentic-ai/skills/project/references/routing-logic.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - routing-logic.md contains the string "Gate 3 still" in the routing table
    - routing-logic.md contains the string "All milestones complete" (terminal row still present and unchanged)
    - The new row appears BEFORE "All milestones complete" in the file (line number of "Gate 3 still" is less than line number of "All milestones complete")
    - routing-logic.md contains the string "sentinel" in the Artifact Validation section
    - routing-logic.md contains "(closed by /project)" as an example in the sentinel guard text
    - bash cicd/lint-markdown.sh exits 0
  </acceptance_criteria>
  <done>routing-logic.md has the Gate 3 closure row inserted before the terminal row; Artifact Validation section has a sentinel path guard that skips the file-existence check for paths starting with `(`.</done>
</task>

<task type="auto">
  <name>Task 3: Update DESIGN.md DD-3 and REQUIREMENTS.md PROJ-10</name>
  <files>skills/project/DESIGN.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - skills/project/DESIGN.md (read lines 86–130 — the DD-3 section)
    - .planning/REQUIREMENTS.md (read the full PROJ-10 row at line 21)
    - .planning/phases/11-gate3-closure/11-RESEARCH.md (Pattern 4, Pattern 5, Pitfall 2)
  </read_first>
  <action>
Make one edit to DESIGN.md and one edit to REQUIREMENTS.md. These are independent — order does not matter.

**Edit 1 — DESIGN.md DD-3: add Gate 3 closure exception paragraph.**
Find the Bootstrap exception paragraph (currently at lines 100-103):
```markdown
**Bootstrap exception:** On first run, if the project-level `progress.txt` does not exist,
`/project` creates it with gate entries and no milestones. Milestone-level `milestone-status.txt`
files are created by `/milestone` when milestones are defined. This bootstrap is the only case where
`/project` writes to disk. After bootstrap, `/project` is strictly read-only.
```
Immediately after this paragraph (and before the "**Artifact validation:**" paragraph), insert a blank line then the new Gate 3 closure exception paragraph. The exact text to insert (verbatim, per D-08):
```markdown
**Gate 3 closure exception:** When all milestones are `[x]` complete and Gate 3 remains
`[~] In progress`, `/project` offers closure via `AskUserQuestion`. This is the only
post-bootstrap write `/project` performs.
```
Do not modify the Bootstrap exception paragraph. Do not modify the Artifact validation paragraph. Do not change any other part of DESIGN.md.

**Edit 2 — REQUIREMENTS.md PROJ-10: add exception parenthetical.**
Find the current PROJ-10 line:
```
- [x] **PROJ-10**: `/project` remains strictly read-only after bootstrap — never modifies state files in normal operation
```
Replace with (per D-09):
```
- [x] **PROJ-10**: `/project` remains strictly read-only after bootstrap — never modifies state files in normal operation (Bootstrap and Gate 3 closure are the two exceptions)
```
Do not modify any other requirement row.
  </action>
  <verify>
    <automated>grep -n "Gate 3 closure exception" /Users/christian/git-repos/OCC-github/agentic-ai/skills/project/DESIGN.md && grep -n "Gate 3 closure are the two exceptions" /Users/christian/git-repos/OCC-github/agentic-ai/.planning/REQUIREMENTS.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - DESIGN.md contains the exact string "Gate 3 closure exception" in the DD-3 section
    - DESIGN.md still contains "Bootstrap exception" (the original paragraph is preserved)
    - DESIGN.md still contains "Artifact validation" after the Gate 3 closure paragraph (insertion order is correct)
    - REQUIREMENTS.md PROJ-10 row contains the exact string "Gate 3 closure are the two exceptions"
    - REQUIREMENTS.md still contains "PROJ-10" (row not deleted)
    - bash cicd/lint-markdown.sh exits 0
  </acceptance_criteria>
  <done>DESIGN.md DD-3 has both the Bootstrap exception and Gate 3 closure exception paragraphs; REQUIREMENTS.md PROJ-10 names both exceptions in a parenthetical. All three documentation locations (SKILL.md Rules, DESIGN.md DD-3, REQUIREMENTS.md PROJ-10) now consistently describe the same narrowed read-only rule.</done>
</task>

</tasks>

<verification>
Run full suite after all tasks complete:

```bash
grep -q "Gate 3 closure" skills/project/SKILL.md && \
grep -q "two exceptions" skills/project/SKILL.md && \
grep -q "Gate 3 closure" skills/project/DESIGN.md && \
grep -q "Gate 3 still" skills/project/references/routing-logic.md && \
grep -q "sentinel" skills/project/references/routing-logic.md && \
grep -q "Gate 3 closure are the two exceptions" .planning/REQUIREMENTS.md && \
echo ALL_PASS || echo FAIL
```

Also verify "sole exception" is gone from SKILL.md:
```bash
grep -c "sole exception" skills/project/SKILL.md
```
Expected: 0. Any non-zero result means the old text was not replaced.

Run markdown lint:
```bash
bash cicd/lint-markdown.sh
```
Expected: exit 0.
</verification>

<success_criteria>
1. `/project` SKILL.md Step 5 contains a Gate 3 closure offer block that fires when all milestones are `[x]` and Gate 3 is `[~] In progress`
2. The offer uses `AskUserQuestion` with "Close Gate 3" and "Leave open" options (D-04)
3. When "Close Gate 3" is selected, `progress.txt` is updated with `[x] Gate 3: Milestone Review  Approved: <date>  (closed by /project)` and routing falls through to "Project complete"
4. When "Leave open" is selected, routing falls through to "Project complete" with no write
5. routing-logic.md new row is positioned before "All milestones complete" (first-match semantics preserved)
6. routing-logic.md sentinel guard prevents PROJ-04 from warning about `(closed by /project)` on re-invocation
7. DESIGN.md DD-3 has the Gate 3 closure exception paragraph (verbatim D-08 text)
8. REQUIREMENTS.md PROJ-10 has the exception parenthetical (verbatim D-09 text)
9. SKILL.md Rules section says "Two exceptions exist" (not "sole exception")
10. bash cicd/lint-markdown.sh exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/11-gate3-closure/11-01-SUMMARY.md` with:
- What was implemented (4 files, specific changes per file)
- Decisions honored (D-01 through D-09 from CONTEXT.md)
- Verification results (full suite command output)
- Any discretion choices made (AskUserQuestion wording, status display after write)
</output>
