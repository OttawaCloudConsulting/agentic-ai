# Milestone 01: Sandboxed Pod
# Status: [~] in progress  6 features, 3 complete

## Features

[x] Feature 01.1: Pre-build verification, provider governance and egress discovery
    Plan: .project/sandboxed-agent-containerization/milestones/01-sandboxed-pod/plans/pre-build-verification-provider-governance-and-egress-discovery.md
    Sub-features: 3/3 complete
    Notes: Completed 2026-09-04.

[x] Feature 01.2: Pod topology, hardened runtime and minimal profile
    Plan: .project/sandboxed-agent-containerization/milestones/01-sandboxed-pod/plans/pod-topology-hardened-runtime-and-minimal-profile.md
    Sub-features: 5/5 complete
    Notes: Completed 2026-09-04. 3 architectural deviations recorded (Deviation 1: single
    multi-stage images/Dockerfile with target: selection; Deviation 2: native agent sandboxes
    disabled by default, D14 amendment needed; Deviation 3: egress-net not a live Docker
    resource until 01.3 attaches a service to it). Consolidated into ARCHITECTURE_AND_DESIGN.md
    on 2026-09-07 (commit 162faec, with 882b86c finishing data flow, resource inventory and the
    file tree) -- no /design refresh outstanding.

[x] Feature 01.3: Egress mediator
    Plan: .project/sandboxed-agent-containerization/milestones/01-sandboxed-pod/plans/egress-mediator.md
    Sub-features: 8/8 complete
    Notes: Re-planned 2026-09-05, Gate 4 re-approved (supersedes the 2026-09-04 approval, which the
    /milestone revision invalidated). Identity, R8.8 and T34 moved to Feature 01.6; 01.3 keeps L7
    controls, DNS, audit, the non-uniform listener surface (claude/agy https, codex plain http
    CONNECT) and the proxy-hop trust anchors, on network-derived identity. T28 amendment stays 01.3's
    and re-homed to SF-3. Codex adversarial pass: 8 findings, 6 fixed, 2 downgraded from blocking with
    operator ratification. Two re-plans queued behind this: 01.1 (deny_fqdns contradiction, carried
    from the prior gate) and 01.4 (stale against Interface Contract 2 -- CODEX_CA_CERTIFICATE, mTLS
    client keys, SF-7a). See docs/records/r8-8-identity-mechanism-gap-escalation.md.
    Completed 2026-09-07. Test command passes: bash tests/acceptance/verify-egress-mediator.sh,
    77 assertions across phases A-G. 11 architectural deviations recorded on the plan (6-11 added
    at the SF-7/SF-8 build). Two mediator defects were found by the harness and fixed rather than
    worked around: Squid's cache_peers are DEAD at start and cost the first fronted request a 500
    (the entrypoint now warms the cascade), and unbound answers RFC 6761 special-use TLDs itself
    and never forwards them (docs/records/resolver-verification.md addendum -- worth a
    compile-time warning in 01.5). All 11 consolidated into ARCHITECTURE_AND_DESIGN.md on
    2026-09-07 (commits 162faec and 882b86c) -- no /design refresh outstanding. The 01.1 deny_fqdns
    re-plan queued here is also closed: recorded as a SUPERSEDED block in 01.1's plan rather than
    re-planned, since 01.1's built artifacts were correct and only its prose was stale.

[~] Feature 01.4: Agent authentication and state persistence
    Plan: .project/sandboxed-agent-containerization/milestones/01-sandboxed-pod/plans/agent-authentication-and-state-persistence.md
    Sub-features: 4/5 complete
    Build started 2026-09-07. SF-1 complete (commit fbe5e06): R4.5 config seed + merge, host-side
    scrub-gitconfig.sh, host-gitconfig override, .gitignore/tmutil, and the pod-topology harness
    extensions (parameterized mount-set, volume exclusivity, auth-surface assertions).
    SF-2 complete 2026-09-07, built in the plan's own sanctioned two-part split.
    SF-2a (commit 56a318e): dispatcher, seven-cell matrix, exits 2/3/4, compose AUTH_MODE seam,
    profile enum surface, T24 amendment, architecture file-tree correction.
    SF-2b: both oauth-interactive cells run live against the pod and
    the endpoint set DERIVED from the audit log, not assumed. Added platform.claude.com (required
    -- claude fails startup without it) and auth.openai.com (four device-code sessions). REMOVED
    claude.ai, console.anthropic.com and platform.openai.com -- all three were candidate guesses
    that the completed flows never contacted. NOT added: mcp-proxy.anthropic.com and
    downloads.claude.ai, both attempted and denied with the login succeeding regardless
    (attempted is not required; R10.3 covers the second). Two implementation defects were found
    by running the cells rather than reasoning about them: the exit-4 probe was unanchored on the
    TLS hop and matched the wrong curl field, so it could never fire; and codex needs
    --device-auth, because plain `codex login` strands on an in-container callback port the host
    browser cannot reach. The claude cell passed while carrying that same second bug. 2 architectural deviations recorded (1: image-tree paths corrected
    to 01.2's multi-stage Dockerfile; 2: absent credential warns at start, fatal only on explicit
    invocation -- operator decision, prevents a verify-pod-topology.sh regression).
    SF-3 complete 2026-09-07. BOTH providers ROLL the refresh token on refresh -- measured, not
    assumed: claude's refreshToken and codex's tokens.refresh_token both changed, each cross-validated
    against a mediator audit line for the refresh endpoint (platform.claude.com 6728/2119 at
    21:32:58Z; auth.openai.com 12492/2168 at 21:35:58Z). Neither endpoint needed an allowlist change --
    SF-2 added both off the LOGIN flow and this is the first evidence the REFRESH flow uses the same
    two hosts. Also measured: claude's refreshTokenExpiresAt is NOT extended by a refresh (same
    absolute instant before and after), so the family dies ~28 days after the original login however
    often it refreshes; codex's refresh trigger is the access token's own 10-day JWT exp, NOT the
    last_refresh field (backdating it 60 days was inert, as was backdating the id_token exp), and
    codex does not verify that JWT's signature locally. SF-4 gate answered: oauth-mount is a one-shot
    bootstrap. Deliverables: docs/records/credential-inventory.md (new, Contract 7, 8 rows incl. the
    not-yet-populated secret-mounted path, 02.5 column empty by design) and the rotation section
    appended to docs/records/agent-verification.md. 1 architectural deviation recorded (3: criterion
    8's cross-client invalidation half is recorded as UNMEASURED by operator decision -- the only
    test is replaying a superseded refresh token against the operator's live account. R4.17 is
    narrowed, not closed, and SF-4 must write accepted_risk.rotation against the conservative
    reading).
    SF-4 complete 2026-09-07. oauth-mount built and its shape constraints VERIFIED by running
    them, not by reasoning: steady state with no source exits 3 and FAILS THE CONTAINER START
    (criterion 5, confirmed -- `codex --version` never ran); a rw-mounted source is refused from
    /proc/self/mountinfo (R4.13); a source without accepted-risk.yaml, or missing any of its five
    fields, is refused (R4.17); a staged copy still carrying OPENAI_API_KEY is refused (the
    test-validity control); a credential with no refresh_token is refused (edge case 11); a
    populated volume is an idempotent no-op. Smoked with a SYNTHETIC credential and scratch
    volumes -- the operator's live host codex login was never spent and codex itself was never
    run against a copy (that is SF-5's call). All smoke volumes removed. 2 architectural
    deviations recorded, both operator-ratified before building (4: the callback publish ships as
    its own compose/overrides/codex-callback.yaml rather than inside the bootstrap fragment, since
    a fragment cannot be layered by halves; 5: the accepted_risk record reaches the container via
    scripts/stage-oauth-mount.sh from a new profiles/oauth-mount.yaml -- the plan required the
    exit-3 check but named no mechanism, and the profile does not exist inside the container).
    VOLUME HAZARD, kept by operator decision. Live credentials are in sf3_claude-state and
    sf3_codex-state ONLY. Four volumes hold SUPERSEDED refresh tokens -- sf2bd_claude-state,
    sf2bd_codex-state, sf3-claude-pristine, sf3-codex-pristine -- and were kept as authenticated
    sessions for testing. They are usable only until their ACCESS token lapses: codex ~2026-09-17
    (10-day exp), claude 2026-09-08T05:03:59Z (8-hour exp). Past those edges the agent refreshes
    with the superseded token UNATTENDED on first run, which is exactly the replay Deviation 3
    records as untested. SF-4/SF-5 should use the sf3_* pair. sf2bd is down (no -v); sf3's mediator
    is up.
    Notes: Re-planned 2026-09-07 against 01.3 as built; plan approved 2026-09-07 and **Gate 4
    CLOSED the same day** -- all three open items resolved by operator decision (supersedes the
    2026-09-04 approval). Ready to build. The plan consumed per-agent client certificates that 01.3 does not issue
    -- they left with R8.8 for Feature 01.6 at the milestone revision -- and set
    CODEX_CA_CERTIFICATE on an agent that has no TLS hop to anchor. Interface Contract 6 now states
    what each agent actually gets. Sub-feature count unchanged. Three checklist items need the
    operator: the test command's relationship to the two existing harnesses, the throwaway provider
    accounts T24 needs, and confirmation that the OAuth endpoint allowlist addition stays 01.4's.

[~] Feature 01.5: Pack composition, policy compiler and build pipeline
    Plan: .project/sandboxed-agent-containerization/milestones/01-sandboxed-pod/plans/pack-composition-policy-compiler-and-build-pipeline.md
    Sub-features: 0/7 complete
    Notes: Re-planned 2026-09-07 against 01.3 as built; plan approved 2026-09-07 and **Gate 4
    CLOSED the same day** -- all three open items resolved by operator decision (supersedes the
    prior approval). Ready to build. The plan defined a compiler CLI incompatible with the one 01.3 shipped -- adopting it
    would have broken the README, both harnesses and the mediator's own stage-1 self-check -- and
    scheduled a build-context change 01.3 had already made. Contract 6 now extends the shipped CLI
    and keeps the exit-code contract as the real addition; SF-4 shrank accordingly. Also picks up
    the RFC 6761 special-use-TLD warning the Gate 2 refresh assigns here. Sub-feature count
    unchanged. Three checklist items need the operator: the test command's scope, the exit-code
    change against existing callers, and the .dockerignore allowlist extension.

[ ] Feature 01.6: Per-agent workload identity
    Plan: (not yet planned)
    Notes: Created 2026-09-04 by /milestone revision -- split out of 01.3. Closes R8.8 with the
    strongest identity form each agent's client supports; carries T34 and the Milestone 03
    brokering gate. Ordered after 01.3; may land before or after 01.4 and 01.5.
