# Milestone 02: Proven and Composable
# Status: [~] in progress  5 features, 3 complete

## Features

[x] Feature 02.1: Audit completeness — agent action log, sink and exports
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/audit-completeness-agent-action-log-sink-and-exports.md
    Sub-features: 5/5 complete
    Notes: Started 2026-09-10. Completed 2026-09-10.

[x] Feature 02.2: Adversarial boundary validation
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/adversarial-boundary-validation.md
    Sub-features: 6/6 complete
    Notes: Started 2026-09-10. Completed 2026-09-11. Deviation 2 recorded (SF-2, in_agent_authed + proxy-leg TLS flags). SF-4 harness/fixture/record committed 2026-09-11 (Phase 8, BOUNDARY_LIVE_INJECT=1 gate); live injected-repo session run 2026-09-11 for claude/codex against the operator's real state volumes -- both refused the exfil step (attempted=false, valid per R15.1). agy leg did not run (no GEMINI_API_KEY in this environment); recorded as a named gap, not a design finding, per operator decision to close SF-4 anyway. See docs/records/boundary-validation.md. SF-5 harness (Phase 9, BOUNDARY_SHADOW_RUN=1) and its live shadow run both completed 2026-09-11: all four single-source allowlist entries (api.anthropic.com, chatgpt.com, api.github.com, github.com) corroborated by the built mediator's own trail (verdict=allow, correct identity_source, http_status=200); provisional flipped true->false in policy/allowlist.base.yaml, lint-policy.sh, and four resolved profiles (default/github/kubernetes/terraform -- Deviation 3, wider than Interface Contract 6's stated one-file scope). codex's planned git-fetch exercise substituted a direct curl to github.com/api.github.com (/workspace held no repo, an environment fact, not a boundary result). See docs/records/boundary-validation.md. SF-6 (Phase 10, T16 + SC-1/SC-2/SC-3) committed 2026-09-11; README updated (not lifted, per Decision 9 -- 02.3/02.4/02.5 still building). Verification surfaced and fixed a drifted policy/resolved/test-fixtures.yaml and test-selfcheck.yaml (SF-3's rotating.fixture.lab entry never recompiled -- one-line fix, recompiled and committed). Standalone validate-boundary.sh run (verify-pack-composition.sh's own drift, unrelated to 02.2, blocks the plan's literal composite Test Command -- see Deviation 4 below): 95/97 assertions pass, reproduced identically across two runs. The 2 failures are the CDN-rotation scenario (T6, SF-3, pre-existing) -- security property holds (attempt 1 allows, attempt 2 refused post-resolution on the correct control/reason) but neither verdict line carries resolved_ip, so the suite's own field assertion fails; recorded as a Decision-9 design finding, routed to /milestone, not patched (mediator's own audit-line emission, out of 02.2 scope). T16 and SC-1/SC-2/SC-3 are unaffected by the T6 finding (T6 not in either's mapped test_id set; failed T6 rows carry egress_logged:false, excluded from T16's join by construction). See docs/records/boundary-validation.md for the full finding writeup.
    Deviation 4 (not yet in a consolidation pass): the plan's Test Command is 02.1's composite plus validate-boundary.sh, ending in scripts/lint-policy.sh -- but verify-pack-composition.sh (shared across features) currently fails on Feature 02.3 in-progress work (R7.6/R5.4/R14.1 message-text and exit-code mismatches on the sf7-probe pack fixture, plus its own independent drift finding on the same two resolved artifacts, found and fixed here). 02.2's own new code (validate-boundary.sh, including SF-6's Phase 10) was verified standalone instead, since the literal composite cannot complete until 02.3 lands. Flagged for the milestone's consolidation pass.

[x] Feature 02.3: Tool pack set, use-case profiles and MCP inventory
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/tool-pack-set-use-case-profiles-and-mcp-inventory.md
    Sub-features: 8/8 complete
    Notes: Started 2026-09-10. Completed 2026-09-11. SF-8 gated on Feature 02.2 reaching [x] (gate-3-review.md: 02.2 before 02.3). SF-1-SF-7 do not consume 02.2 output. Deviation (not recorded, implementation-detail): pack-install.sh COPY moved from agent-base to agent-packs in images/Dockerfile (SF-2) -- agent-base is GHCR-published and digest-pinned (D21), so a local script edit reached nothing until moved, same class of fix as entrypoint.sh (01.6 SF-3). Deviation (not recorded, implementation-detail, SF-3): scripts/check-profile-compose.sh's normalise() stripped `.build.args.MEDIATOR_PROFILE`, which does not exist -- MEDIATOR_PROFILE renders under `.environment`, not `.build.args`. Fixed to del `.environment.MEDIATOR_PROFILE`; the SF-2 script never hit this path because `check-profile-compose.sh --profile default` compares default to itself, where MEDIATOR_PROFILE is identical either way. Deviation (not recorded, implementation-detail, SF-5): `check-profile-compose.sh`'s `secrets_of()` compared `docker compose config`'s rendered secrets with a bare `yq eval '...secrets[]'`, which emits each entry as a two-line `{source, target}` mapping (Compose v2.38.2 normalizes every secret reference to long form, even ones written short) -- so line-based `comm`/`sort` treated `source:`/`target:` as separate set members and every profile carrying a pack credential (including the already-shipped `github`) reported a false secret-set mismatch; the (b) hardening-comparison half never actually passed for `github` either, because Phase B only ever exercised `check-profile-compose.sh` against `default`-vs-itself. Fixed to extract `.source` via `yq -o=json | jq`, one name per line regardless of rendered form; both `github` and the new `kubernetes` profile now pass. Real HashiCorp/GitHub-pattern SHA-256 checksums for `kubectl` (`dl.k8s.io/release/v1.31.2/.../kubectl.sha256`) and `helm` (`get.helm.sh/.../....sha256sum`) fetched and verified against a live build, per `packs/README.md`'s "How these hashes were obtained" pattern.
    SF-8 (2026-09-11): BOUNDARY_PROFILES matrix built into validate-boundary.sh (terraform/kubernetes/github/default, default forced last per Contract 7); scratch test-base compiles via Decision 10's mechanism (--profile-file already existed from 02.1 SF-1, so that named interface-change item is a no-op here); new Phase 11 (credential reachability, pack-egress allow/deny, /run/secrets delta against a captured default baseline). tests/fixtures/authoritative-dns/unbound.conf gained fixture-collector-backed records for the four real pack FQDNs. Two pre-existing shared-harness reconciliations, recorded as Deviations, both required for "Composite green": (1) verify-pack-composition.sh's sf7-probe fixture needed a third_parties placeholder (02.3 SF-1's new R14.1 gate) and one stale expected-message fragment fixed. (2) T6's resolved_ip-on-deny gap (02.2 Decision 9) stays unfixed -- a real fix needs a new external_acl_type helper in the mediator's deny path, judged out of SF-8's scope after live investigation; validate-boundary.sh now excuses only that field's absence on an otherwise-fully-verified-correct deny. Live re-exercising this scenario surfaced a SECOND, previously unrecorded finding (attempt 1's front-cold-peer 500, unrelated to the profile matrix -- reproduces under BOUNDARY_PROFILES=default alone): two retry-based fixes were tried and reverted because each one measurably broke attempt 2 instead (Squid connection-pool state left warm by the extra touch); the shipped fix recognizes the symptom on attempt 1's own line and records it as a known gap, no extra request added. Full matrix + full 7-harness composite (verify-pack-composition, verify-pod-topology, verify-egress-mediator, verify-audit-completeness, verify-tool-packs, verify-mcp-inventory, lint-policy) run clean end-to-end. See docs/records/boundary-validation.md for the full per-profile results and both findings' writeups, docs/records/mcp-inventory.md for the T31 assessment and github.com residual, and docs/records/third-party-assessments.md for the T42 re-inspection.

[~] Feature 02.4: Reproducibility, provenance and onboarding
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/reproducibility-provenance-and-onboarding.md
    Sub-features: 8/10 complete
    Notes: Started 2026-09-11. SF-1 (2026-09-11): NODE_BASE_DIGEST pinned (index digest of
    node:22-slim) at both images/Dockerfile FROMs; agent-base's ca-certificates/curl/
    bubblewrap/iproute2/xz-utils moved onto the same bookworm snapshot as git (SF-1b);
    mediator's squid-openssl/unbound/dnsdist/ca-certificates moved onto a dated trixie
    snapshot via a new keyring, images/keyrings/debian-archive-trixie.gpg (SF-1c).
    Found and fixed: DNSDIST_VERSION=1.9.16-0+deb13u1 never existed in Debian's trixie
    archive (live or any snapshot back to 2026-08-15) -- corrected to 1.9.15-0+deb13u1,
    the version that was actually resolving. apt-pinned.sh gained --allow-downgrades
    (needed whenever a pinned snapshot version is older than a bootstrap install already
    on the image from live default sources). verify-reproducibility.sh created with
    phase A (no FROM resolves to a mutable tag), negative control run and passing.
    Pushed to feature/containerization; CI run 34640174753 published a new agent-base
    (branch build, for testing only); AGENT_BASE_DIGEST repinned to
    sha256:f5fd8aa9718aa5ca63520db2cdc436b0554b2ac58182e1b3dfe912c323a045c4; full 02.3
    composite (verify-pack-composition through lint-policy, plus verify-reproducibility)
    re-run and green end to end. T45/head_branch is expected to still fail until SF-4
    promotes to main -- not yet checked (that's SF-2's phase C).
    SF-2 (2026-09-11): verify-reproducibility.sh gained phase B (images/build-allowlist.yaml,
    7 non-pack hosts: registry-1.docker.io, ghcr.io, github.com, snapshot.debian.org,
    deb.debian.org, registry.npmjs.org, storage.googleapis.com -- extracted mechanically from
    both Dockerfiles/pins.env and checked against the allowlist union the four shipped packs'
    egress.build.allow_fqdns) and phase C (T45's digest -> SBOM -> provenance -> run chain, via
    anonymous `docker buildx imagetools inspect --format '{{json .SBOM}}'/'{{json .Provenance}}'`
    against ghcr.io, then the public runs API). Ran against the SF-1 branch digest
    (sha256:f5fd8aa9718aa5ca63520db2cdc436b0554b2ac58182e1b3dfe912c323a045c4, run 34640174753):
    14 passed, 1 failed -- SF-2's own closing condition (every T45 assertion passes except
    head_branch, which is 'feature/containerization' not 'main', EXPECTED RED until SF-4).
    Found and fixed while building phase B: yq v4.53.6 rejects `[]?.fqdn // empty`
    (`lexer: invalid input text "empty"`) -- rewritten as `[].fqdn` per pack file, since every
    shipped pack.yaml already declares a non-empty egress.build.allow_fqdns.
    SF-3 (2026-09-11): README Bring-Up rewritten as one ordered 1-7 sequence (prerequisites,
    clone, install-deps.sh, all eight issue-identity.sh commands including the three previously
    missing -- client claude, credential codex, credential agy -- canonical up, first-run auth,
    verify). Normalized every documented `docker compose ... up` invocation in README.md to the
    one canonical `--build --force-recreate` form (found and fixed: two conflicting forms at the
    entry point/Bring-Up and in "Adding or removing a pack", plus `-d` on four more layered-
    fragment invocations -- oauth-mount steady state, host-gitconfig, build-cache, the two
    `github` profile switches). Added `agy`'s `GEMINI_API_KEY` delivery path (`exec -e`/`run --rm
    -e`, host shell only, paid-tier note) -- corrected the plan's cited `bootstrap-auth.sh:43` to
    the actual `:215`. Added "Changing the allowlist" (R12.3, reviewer role, corrected from the
    plan's assumption that R14.1's third_parties record applies to `policy/allowlist.base.yaml` --
    it only applies to pack manifests, verified against `lint-policy.sh`/`compile-policy.sh`) and
    "Updating an agent version" (R10.6, citing `check_pin_agreement()` in
    `verify-pod-topology.sh`, not a standalone script as the plan assumed). `install-deps.sh`
    gained `--with-sbx` (sbx no longer checked by default, D17/R11.3). `packs/README.md` gained a
    one-line pointer to `images/build-allowlist.yaml`. `verify-reproducibility.sh` gained phase D
    (T39): no unrecorded wrapper script invokes the real pod with up/start/run (one recorded
    exception -- `scripts/run-shadow-run-sf5.sh`, 02.2 SF-5's live boundary-verification harness);
    every README `docker compose` line carries `--env-file`; identity commands and listener IPs
    agree with `mediator/identity/README.md` and `compose.yaml`. Found and fixed: the top-level
    entry-point code fence was untagged (plain ``` ```, not ` ```bash `), which silently excluded
    it from phase D's scan -- retagged, and a negative control (an injected `up -d`) confirmed
    the check then catches it. Standalone run: 19 passed, 1 failed (phase C's pre-existing
    EXPECTED RED, unaffected by SF-3).
    SF-4 (2026-09-11): PR #39 (feature/containerization -> main, D21 promotion) prepared and
    merged by operator (commit 6d39fe93f567455f16dade84d458b6f03cc95358, run 34652870008).
    Published digest confirmed via `docker buildx imagetools inspect` against ghcr.io directly
    (sha256:bc8d16b022b41377c0e1704387adf153199830d03fcabb4f4b97ca206b87d855) -- the workflow's
    "Report the digest to pin" step's env-context log lines echo pins.env's stale pre-repin
    value, not the run's actual output; the step's own stdout (steps.build.outputs.digest)
    carries the correct value and agrees with the direct GHCR query. AGENT_BASE_DIGEST repinned
    on branch feature/repin-main-agent-base, PR #40, per Decision 1 step 5 (repin lands through
    PR, not a direct main commit); operator merged (commit c2d384c10586d9e0f22dd1edf20ee8f3eb797f37).
    verify-reproducibility.sh standalone: 20/20 pass, including Phase C's head_branch=='main'
    (previously the one EXPECTED RED). Full feature composite (verify-pack-composition through
    lint-policy) re-run clean end-to-end against the main digest, including the two pre-existing
    recorded gaps (T6 cold-peer 500, resolved_ip-on-deny) with no new failures. D21 trigger
    amendment drafted in PR #39's description; not yet applied to ARCHITECTURE_AND_DESIGN.md
    (SF-6 records it, applying it is out of this feature's scope). Completed 2026-09-11.
    SF-5 (2026-09-11, partial): scripts/fingerprint-environment.sh (Contract 5) built and
    verified against the currently-built default profile's images (dpkg-query is absent from
    the final agent image per R7.19 condition 3 -- fell back to parsing the retained
    /var/lib/dpkg/status; npm is likewise stripped -- hashes /usr/local/lib/node_modules
    content directly instead of running `npm ls`). docs/records/reproducibility.md created
    with the SF-5 "Clean-environment procedure" runbook only (Stages 1-5); T45/R10.4/R11.2/
    T18/T39/architecture-edit sections deferred to SF-6 per the plan. NOT closed: the plan's
    reference commit X must carry this script, so X is a future PR-to-main merge commit (same
    operator-gated path as SF-4), not the existing SF-4 commit -- surfaced to and confirmed by
    operator rather than assumed. The physical run itself (Stages 1-5 on a second physical
    Mac, macOS 26/Apple silicon) is operator-only and has not happened. Sub-feature count
    stays 4/6 until that run completes and the fingerprint diff + Test Command both pass.
    SF-5 run (2026-09-12/13): performed on the second Mac at X. T18 NOT passed -- Test Command
    passed (6 of 9 scripts by operator attestation), but the fingerprint diff is not clean on any
    profile. F1: reference recipe omitted AGENT_PROFILE, so the terraform/kubernetes/github
    references fingerprint default packs, and fingerprint-environment.sh never passes AGENT_PROFILE
    to its own compose config call. F2: mediator transitive packages float with the live Debian
    archive (libcom-err2 +b11 -> +b12, verified by a no-cache rebuild). F3: compose_config_sha256
    depends on the Compose version (second Mac v5.3.1 vs build host v2.38.2; v5.3.1 on the build
    host reproduces the clean hashes exactly). T39
    findings F4-F10 (runbook defects and run conduct). R11.1 not discharged. F1's fingerprint-script
    gaps, F2 and F3 route to /milestone; new references and a factory-reset full re-run are
    needed. See docs/records/reproducibility.md, SF-5 run results. Sub-feature count stays 4/6.
    Milestone revision (2026-09-13, /milestone revision mode, criteria only): 02.4's T18 criterion
    clarified in README.md after the SF-5 findings -- every profile fingerprinted as built for that
    profile (F1), OS package set independent of rebuild date (F2), comparison independent of Docker
    Desktop/Compose version with versions recorded (F3). Feature NOT reset; plan and SF-1-SF-4 kept.
    Implementation re-plan follows via /plan-feature. Fresh gate-3-review.md generated.
    Re-plan (2026-09-13, /plan-feature re-plan mode, Gate 4 re-approved): Decision 8 and four new
    sub-features ahead of the SF-5 re-run -- SF-5a fingerprint v2 (F1 profile-aware render + exit-4
    profile check via /opt/agent-pack/profile.json, F3 normalised Compose JSON + version fields),
    SF-5b mediator/recorder OS packages from the dated snapshot (F2, local), SF-5c agent-base from
    the snapshot with republish + repin (F2, operator-gated), SF-5d clean-environment procedure
    revision 2 (F1, F4-F10). Contract 5 revision 2, Contract 4 addendum, new Contract 7, Edge Cases
    16 corrected and 17-19 added. Sub-features 6 -> 10; SF-1-SF-4 remain complete. Status kept [~]
    rather than reset to [ ]: the re-plan spec covers planned-awaiting-build and pending features
    only, and 02.4 was already in progress with built sub-features. Prior Gate 4 review preserved as
    reviews/gate-4-reproducibility-provenance-and-onboarding-review-2026-09-10.md.
    SF-5a (2026-09-13): fingerprint-environment.sh to Contract 5 revision 2 -- schema 2,
    compose_config_sha256 = sha256 of `AGENT_PROFILE="$PROFILE" compose ... config --format json`,
    <ROOT>-normalised, `del(.. | .create_host_path?)`, `jq -S`; docker_version/compose_version
    recorded; exit 4 when any of the three agent images' /opt/agent-pack/profile.json is absent or
    names another profile (read out with cat and parsed on the host -- jq is absent from the
    claude/codex images). images/Dockerfile agent-packs writes profile.json ({"profile","packs"},
    pack names sorted from pack-plan's packs.txt; PROFILE redeclared as a stage ARG and checked as
    a ^[a-z0-9][a-z0-9-]*$ token before it is printed into JSON, since pack-plan only checks the
    profile file exists). COMPOSE_BIN (optional standalone Compose binary) added solely to drive the
    v5.3.1 negative control through the shipped code path. Negative controls, run once on the build
    host: (1) pre-change :local images -> exit 4 "records no profile"; (2) `fingerprint github`
    against AGENT_PROFILE=default-built images -> exit 4 "built for profile 'default', not
    'github'"; (3) `fingerprint default` under Compose v2.38.2 and under COMPOSE_BIN=v5.3.1
    (sha256-verified release binary) -> both exit 0, compose_config_sha256 f5827457... on both, full
    documents diff clean with commit/docker_version/compose_version excluded. The normalised render
    pipeline, run by hand with both binaries, gives equal hashes for all four shipped profiles
    (default f5827457, terraform 62452ef4, kubernetes 9cad75c2, github 3cb34c1b) -- and default no
    longer equals terraform, so the render now distinguishes pack selection (F1). Non-default
    propagation (the controls above all used default, which is also the ARG fallback): rebuilt with
    AGENT_PROFILE=github -> all three images carry {"profile":"github","packs":["github-cli",
    "language-runtimes"]}; `fingerprint github` exit 0 (compose_config_sha256 3cb34c1b..., matching
    the hand-run pipeline); symmetric control `fingerprint default` against those images -> exit 4.
    terraform/kubernetes not rebuilt; their script-level run happens with the SF-5 references.
    :local agent images are left github-built. No test or image script enumerates /opt/agent-pack
    (fixed-path reads only, entrypoint.sh:150/170/195), so profile.json breaks nothing there.
    pack_roots_sha256 now covers profile.json (schema-1 references were already void).
    Mediator dpkg_sha256 still 800c5839... (the F2 drift) -- SF-5b's scope, untouched here.
    SF-5b (2026-09-13): mediator bootstrap (ca-certificates/curl/gpgv) and recorder jq moved off
    the base image's live default sources onto the dated snapshot per Contract 7 (default sources
    removed first, http, signed-by the committed keyring). Deviation 1 recorded (operator
    decision): Contract 7 as written, one main-archive snapshot, dropped security updates -- the
    recorder build failed (JQ_VERSION 1.7.1-6+deb13u3 exists only in trixie-security) and the
    mediator's OpenSSL downgraded 3.5.7 -> 3.5.6. Fix: a second dated source, debian-security
    trixie-security at the same timestamp (MEDIATOR_SECURITY_SNAPSHOT_URL/_SUITE/_FINGERPRINT in
    pins.env), security key committed as images/keyrings/debian-security-trixie.gpg (byte-identical
    to the base image's debian-archive-trixie-security-automatic.pgp), check-valid-until=no on
    that source only (its InRelease has a 7-day Valid-Until; apt refuses an expired one, measured
    against the 20260801 security snapshot). apt-pinned.sh gained an optional leading --security
    <keyring> <url> <suite> <fingerprint> (verified per source by VALIDSIG field 12); existing
    callers unchanged -- agent-base target rebuilt locally without the flag and passes. compose.yaml
    passes the snapshot args to the mediator and all three recorders. Coverage measured: every
    package the sf5-ref mediator and the prior recorder add over the base is in main U security;
    trixie-updates adds none. Evidence: no-cache mediator build -- libcom-err2 1.47.2-3+b11 and
    openssl 3.5.7 match sf5-ref; the remaining 3-line diff (libdbi-perl 1.652-2~deb13u1, libexpat1
    2.8.3-1~deb13u1, libtdb1 ...deb13u2) is all trixie-security versions newer than sf5-ref's,
    which lacked them because SF-1c's apt-pinned closure resolved from the main snapshot only. A
    second no-cache rebuild gives an identical list (sha256 e98336d5...), so the package set no
    longer depends on the rebuild date. Recorder package list identical to its pre-change image.
    verify-reproducibility.sh: images/recorder/Dockerfile added to phase A and phase B; phase B
    gained the snapshot-first check (file-order awk: default sources removed + bootstrap-snapshot.list,
    or apt-pinned, before any apt-get install) with an in-suite negative control (install ahead of
    the mediator's snapshot step is refused); the old `ca-certificates curl` grep for deb.debian.org
    is replaced by "a snapshot-first violation is a deb.debian.org fetch site". Standalone run: 24
    passed, 1 failed -- EXPECTED RED on images/Dockerfile:132 (agent-base bootstrap) until SF-5c.
    deb.debian.org NOT removed from build-allowlist.yaml: agent-base still fetches there (the plan's
    "once no fetch site remains" is not yet met); used_by trimmed to images/Dockerfile, and the
    dead-entry check forces removal after SF-5c. SF-5c impact (Deviation 1): agent-base and
    pack-install.sh's apt-pinned calls need bookworm-security + --security the same way. Local
    :local mediator and recorder images are now SF-5b builds. Investigation:
    agents/investigations/02.4-sf5b-snapshot-lacks-security-updates.md (resolved; local only,
    agents/ is gitignored -- plan Deviation 1 carries the same content).
    SF-5c (2026-09-13, partial -- local half, branch publish and branch composite done; main
    promotion and repin pending, operator-gated): agent-base bootstrap (ca-certificates, curl) moved
    off node:22-slim's live sources onto the dated snapshot per Contract 7, main bookworm
    20260901T000000Z + bookworm-security; images/keyrings/debian-security-bookworm.gpg (byte-identical
    to node:22-slim's debian-archive-bookworm-security-automatic.gpg, primary 05AB9034...A8F0);
    SECURITY_SNAPSHOT_* in pins.env, the workflow pin loop/build-args and the three agent services'
    build.args. deb.debian.org removed from build-allowlist.yaml (phase B dead-entry check). Deviation 2
    recorded (operator decisions): (a) security snapshot is 20260910T203409Z, LATER than main -- at
    20260901 a no-cache build downgraded libssh2-1 1.10.0-3+deb12u1 (security upload, present in the
    published base from the live archive) to +b1; (b) apt-pinned.sh records the security source at
    /etc/agent-apt-security-snapshot and pack-install.sh passes --security from it (profile schema
    unchanged; absent record on an older base -> note, main-only, as before). Measured: bookworm-updates
    @20260901 newer than main for no installed package. Evidence: two no-cache agent-base builds
    identical; vs published base only libexpat1 deb12u2 -> deb12u3; terraform agent-packs on the local
    base verifies both InReleases and keeps the security line. Committed 9627782; branch pushed; publish
    via workflow_dispatch run 34797477156 -> sha256:389f689ee8c807b0e8d3d46ce9ae0fb8ec4bf7d15f3c396075272d66a2a03d07,
    package list identical to the local build (127). Full Test Command against that digest (working-tree
    pin only, restored after; shell export does not reach verify-pod-topology.sh, which sources pins.env):
    all harnesses and lint-policy pass with only the two recorded T6 known gaps; verify-reproducibility
    24/1, the 1 = phase C head_branch on the branch digest (expected). Promotion: PR #45 merged by
    operator (squash commit f3a7d2f3643cc3b13d55d730cdd6c6428363805d); main push run 34799978913 published
    sha256:e3ae687e1b432fb2e2fd78620f199b59692e776d7368948ea4196a729d266824 (provenance builder.id = that run,
    vcs:revision f3a7d2f, SPDX SBOM 327 packages); installed package list identical to the branch publish
    and the local build (127). Repin through PR #46 (merged by operator, commit b3e17bb). Before merge:
    verify-reproducibility.sh standalone 25/0 (phase C head_branch == main); full Test Command against the
    committed pin exit 0 -- verify-pack-composition 109/0, verify-pod-topology, verify-egress-mediator,
    verify-audit-completeness pass, verify-tool-packs 41/0, verify-mcp-inventory 44/0, validate-boundary
    (terraform/kubernetes/github/default) pass, verify-reproducibility 25/0, lint-policy OK; only the two
    recorded T6 known gaps, same 16 lines as the branch-digest run. Operator flag, not in scope:
    CA_CERTIFICATES_VERSION still pins the 2023 bundle (security has 20250419~deb12u1). Completed
    2026-09-14.
    SF-5d (2026-09-13): docs/records/reproducibility.md gained "Clean-environment procedure --
    revision 2 (SF-5d)" after the SF-5 run results; the pre-run procedure is unchanged. Fixes F1
    (AGENT_PROFILE on every profile build, default included), F4 (build, not up), F5 (pod down,
    without -v, before the Test Command), F6 (git clean -fdX), F7 (tee wrapper under pipefail, exit
    codes to test-exit.txt; DD-12 adjustment, same scripts/order/gating), F8 (README steps named by
    section and step number, only instrumentation written out, no derived copy), F9 (build output
    to per-profile files; documented log-cleaning command), F10 (teardown only after the build host
    confirms receipt); Stage 2 captured to stage2.txt. Reference commit X2 left as a placeholder:
    a main commit carrying SF-5a-SF-5d and b3e17bb, recorded when the re-run starts; schema-1
    references void. Verified on the build host: build-then-fingerprint interleaved per profile
    (one shared :local tag); no build: block has secrets, and a github `build claude` with an empty
    PACK_CREDENTIALS_DIR exits 0 (cached layers -- shows the secret file is not needed to build);
    Stage 1 reference builds use --no-cache (npm transitives are unlocked, so a cached layer would
    fake an npm_tree_sha256 diff); the wrapper truncates test-exit.txt per run; the tee wrapper stops the chain
    and records composite exit=3 on a failing stand-in script; git clean -ndX mediator/identity/
    removes ca/ clients/ credentials/ listeners/ and keeps README.md/.gitignore. The log-cleaning
    command was verified against a synthetic log only. "What closes SF-5" gained a status note.
    No mechanism change, so no Test Command run (SF-5 and SF-6 remain).

[~] Feature 02.5: Containment, response and the authorization gate
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/containment-response-and-the-authorization-gate.md
    Sub-features: 0/7 complete
