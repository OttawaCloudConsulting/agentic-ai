# Milestone 02: Proven and Composable
# Status: [ ] pending  5 features, 0 complete

## Features

[x] Feature 02.1: Audit completeness — agent action log, sink and exports
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/audit-completeness-agent-action-log-sink-and-exports.md
    Sub-features: 5/5 complete
    Notes: Started 2026-09-10. Completed 2026-09-10.

[~] Feature 02.2: Adversarial boundary validation
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/adversarial-boundary-validation.md
    Sub-features: 1/6 complete
    Notes: Started 2026-09-10.

[~] Feature 02.3: Tool pack set, use-case profiles and MCP inventory
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/tool-pack-set-use-case-profiles-and-mcp-inventory.md
    Sub-features: 6/8 complete
    Notes: Started 2026-09-10. SF-8 gated on Feature 02.2 reaching [x] (gate-3-review.md: 02.2 before 02.3). SF-1-SF-7 do not consume 02.2 output. Deviation (not recorded, implementation-detail): pack-install.sh COPY moved from agent-base to agent-packs in images/Dockerfile (SF-2) -- agent-base is GHCR-published and digest-pinned (D21), so a local script edit reached nothing until moved, same class of fix as entrypoint.sh (01.6 SF-3). Deviation (not recorded, implementation-detail, SF-3): scripts/check-profile-compose.sh's normalise() stripped `.build.args.MEDIATOR_PROFILE`, which does not exist -- MEDIATOR_PROFILE renders under `.environment`, not `.build.args`. Fixed to del `.environment.MEDIATOR_PROFILE`; the SF-2 script never hit this path because `check-profile-compose.sh --profile default` compares default to itself, where MEDIATOR_PROFILE is identical either way. Deviation (not recorded, implementation-detail, SF-5): `check-profile-compose.sh`'s `secrets_of()` compared `docker compose config`'s rendered secrets with a bare `yq eval '...secrets[]'`, which emits each entry as a two-line `{source, target}` mapping (Compose v2.38.2 normalizes every secret reference to long form, even ones written short) -- so line-based `comm`/`sort` treated `source:`/`target:` as separate set members and every profile carrying a pack credential (including the already-shipped `github`) reported a false secret-set mismatch; the (b) hardening-comparison half never actually passed for `github` either, because Phase B only ever exercised `check-profile-compose.sh` against `default`-vs-itself. Fixed to extract `.source` via `yq -o=json | jq`, one name per line regardless of rendered form; both `github` and the new `kubernetes` profile now pass. Real HashiCorp/GitHub-pattern SHA-256 checksums for `kubectl` (`dl.k8s.io/release/v1.31.2/.../kubectl.sha256`) and `helm` (`get.helm.sh/.../....sha256sum`) fetched and verified against a live build, per `packs/README.md`'s "How these hashes were obtained" pattern.

[~] Feature 02.4: Reproducibility, provenance and onboarding
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/reproducibility-provenance-and-onboarding.md
    Sub-features: 0/6 complete

[~] Feature 02.5: Containment, response and the authorization gate
    Plan: .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/containment-response-and-the-authorization-gate.md
    Sub-features: 0/7 complete
