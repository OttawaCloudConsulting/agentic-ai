# Gate 4 Review -- Feature Plan: Reproducibility, provenance and onboarding

**Artifact:** .project/sandboxed-agent-containerization/milestones/02-proven-and-composable/plans/reproducibility-provenance-and-onboarding.md
**Status:** [x] Approved
**Reviewer(s):** Operator
**Date:** 2026-09-13

**Re-plan.** Generated by `/plan-feature` re-plan mode after SF-5's first clean-environment run
(`docs/records/reproducibility.md`, SF-5 run results). Adds Decision 8, SF-5a–SF-5d, Contract 5
revision 2, a Contract 4 addendum, Contract 7 and Edge Cases 17–19. The prior approved review is
preserved unchanged at `reviews/gate-4-reproducibility-provenance-and-onboarding-review-2026-09-10.md`.

## Pre-checks (verified programmatically)

- [x] Plan file exists at the expected path
- [x] All 12 required sections present (Summary through Architectural Deviations)
- [x] `milestone-status.txt` exists for milestone 02
- [x] Feature 02.4 exists in `milestone-status.txt`

## Checklist

- [x] Does the approach handle known edge cases?
- [x] Are the sub-features correctly scoped for single-session work?
- [x] Is the test command appropriate for this feature?
- [x] Are the files to create/modify correct?
- [x] Are interface contracts compatible with existing code?
- [x] [Auto] **Confirm F2 covers every rolling-archive bootstrap.** Mediator (`images/mediator/Dockerfile:188`),
      recorder (`images/recorder/Dockerfile:19`) and `agent-base` (`images/Dockerfile:132`) move to
      Contract 7; the `agy` stage's `jq` is left alone because it already resolves from the snapshot.
- [x] [Auto] **Confirm Contract 7 keeps `apt-pinned.sh` unchanged.** Its `https://`-only rule and
      fingerprint check stay; only the bootstrap's package source moves. `trusted=yes` is never used.
- [x] [Auto] **Confirm Edge Case 18 is checked before SF-5c.** apt's acceptance of the committed keyring
      files under `signed-by` is verified during SF-5b's local builds, before the `agent-base` republish.
- [x] [Auto] **Confirm Contract 5 revision 2 compatibility.** Exit codes 0–3 are unchanged and 4 is new;
      schema-1 references are not comparable with schema 2, so the re-run regenerates all four.
- [x] [Auto] **Confirm SF-5c follows the SF-4 operator-gated path.** Branch publish and test repin, then
      PR to `main`, operator merge, `main` digest captured, repin through PR, phase C green.
- [x] [Auto] **Confirm the re-plan negative controls are recorded, not left in the suite.** Wrong-profile
      exit 4, Compose v2/v5 equal hashes, phase B refusing an early `apt-get install`, and the no-cache
      mediator package list matching `sandboxed-agent/mediator:sf5-ref`.

## Reviewer Comments

(none)
