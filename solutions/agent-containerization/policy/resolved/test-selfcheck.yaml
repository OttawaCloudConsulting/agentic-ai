# GENERATED by scripts/compile-policy.sh -- do not hand-edit (SC-6).
# Edit policy/allowlist.test.yaml, policy/denylist.test.yaml or profiles/test-selfcheck.yaml, then recompile.
# Verify with: bash scripts/compile-policy.sh --check --profile test-selfcheck --allowlist policy/allowlist.test.yaml --denylist policy/denylist.test.yaml
schema: 1
profile: test-selfcheck
compiled_at: 2026-09-10T01:05:35Z
compiled_from:
  allowlist: policy/allowlist.test.yaml
  denylist: policy/denylist.test.yaml
  profile: profiles/test-selfcheck.yaml
  packs: []
provisional: true
pins: agent-verification.md

agents:
  claude:
    identity: claude
    listener_port: 3128
    listener: {scheme: https, tls: true, port: 3128, client_auth: mtls}
    allow_fqdns:
      - {fqdn: allowed.fixture.lab, port: 443, upgrade: false}
      - {fqdn: claude-only.fixture.lab, port: 443, upgrade: false}
      - {fqdn: denied-by-fqdn.fixture.lab, port: 443, upgrade: false}
      - {fqdn: neighbour.fixture.lab, port: 443, upgrade: false}
    allow_cidrs: []
    limits: {max_concurrent: 16, bytes_per_second: 2000000}
  codex:
    identity: codex
    listener_port: 3128
    listener: {scheme: http, tls: false, port: 3128, client_auth: proxy_auth}
    allow_fqdns:
      - {fqdn: allowed.fixture.lab, port: 443, upgrade: false}
      - {fqdn: neighbour.fixture.lab, port: 443, upgrade: false}
      - {fqdn: upgrade.fixture.lab, port: 443, upgrade: true}
    allow_cidrs: []
    limits: {max_concurrent: 16, bytes_per_second: 2000000}
  agy:
    identity: agy
    listener_port: 3128
    listener: {scheme: https, tls: true, port: 3128, client_auth: proxy_auth}
    allow_fqdns:
      - {fqdn: allowed.fixture.lab, port: 443, upgrade: false}
    allow_cidrs: []
    limits: {max_concurrent: 16, bytes_per_second: 2000000}

deny_cidrs:
  - 10.0.0.0/8
  - 127.0.0.0/8
  - 169.254.0.0/16
  - 172.31.40.21/32
  - 192.168.0.0/16
deny_fqdns:
  - denied-by-fqdn.fixture.lab

# Entries present in policy/allowlist.test.yaml and deliberately NOT granted by this profile.
# Recorded rather than dropped silently, so the decision is auditable (criterion 12).
exclusions: []

startup_check:
  allowed: {agent: claude, fqdn: allowed.fixture.lab, port: 443}
  denied: {ip: 169.254.169.254, port: 443}
  offline: false
